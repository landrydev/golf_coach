# Production mutation route audit-event matrix

This is the first-party completeness contract for production HTTP mutation routes. A listed action is the privacy-safe `audit_events.action` written when the corresponding durable business transition commits successfully; conditional actions are emitted only when that transition occurs. The matrix is enforced by `tests/audit-event-matrix.test.mjs`, which discovers route methods, checks producer wiring, and requires every mapped action literal to remain in its named production source.

Authenticated routes may additionally emit the cross-cutting identity actions `account.created`, `account.activated`, or `identity.mapping_updated` while reconciling the trusted Sites identity. Those actions describe identity/account lifecycle, not the route-specific business mutation, so they are not repeated in every row.

`[TRANSPORT-ONLY]` marks state that deliberately has no first-party audit event because it only authenticates, serializes, retries, deduplicates, or cleans up transport and does not assert a new product, payment, access, or entitlement outcome. `[IDEMPOTENT NO-OP]` marks a successful response that committed no new state and therefore must not create a duplicate event.

## Enforced route matrix

<!-- AUDIT_EVENT_MATRIX_START -->
| Method | Route | Route producer marker(s) | Expected success audit action(s) | Audit source(s) | Deliberate no-new-audit behavior |
|---|---|---|---|---|---|
| `DELETE` | `/api/account/shares/[shareId]` | `revokeShareLink` | `share.revoke` | `lib/plans.ts` | `[IDEMPOTENT NO-OP]` The tenant-scoped account-containment route exposes no golfer content or bearer and revokes every session under the named link. An already inactive own link changes no state and emits no duplicate event; another tenant's identifier returns not found. |
| `POST` | `/api/billing/checkout` | `expireStrandedCheckoutReservation`<br>`expireCheckoutAttemptAfterProviderConfirmation`<br>`markCheckoutAttemptCompletedPendingSync`<br>`finalizeCheckoutAttemptOpen` | `billing.checkout_reservation_expired`<br>`billing.checkout_session_expired`<br>`billing.checkout_completion_pending_sync`<br>`billing.checkout_session_created` | `lib/checkout-repository.ts` | `[TRANSPORT-ONLY]` Provider-call reservation, account-operation leases, bounded retry/error fields, and reuse of an already-audited open session are concurrency/transport state. They create no charge or entitlement and add no audit row. |
| `POST` | `/api/billing/portal` | `recordHostedBillingSession` | `billing.portal_session_created` | `lib/billing-repository.ts` | — |
| `POST` | `/api/billing/reconcile` | `reconcileBillingAccount` | `billing.checkout_session_expired`<br>`billing.checkout_completion_pending_sync`<br>`billing.reconciliation_checked`<br>`billing.subscription_reconciled` | `lib/checkout-repository.ts`<br>`lib/billing-repository.ts` | — |
| `POST` | `/api/billing/webhook` | `applyStripeSubscriptionEvent` | `billing.subscription_synced` | `lib/billing-repository.ts` | `[TRANSPORT-ONLY]` Signed receipt, processing lease, duplicate acknowledgement, and ignored/non-actionable disposition stay in `billing_events`. Without a subscription projection they add no second audit row. Processing failure is separately recorded as `billing.webhook_failed`. |
| `POST` | `/api/coaching/drills` | `createDrillTemplate` | `drill_template.created` | `lib/rich-coaching.ts` | The tenant-scoped template insert and audit event commit in one D1 batch. |
| `DELETE` | `/api/coaching/drills/[drillId]` | `archiveDrillTemplate` | `drill_template.archived` | `lib/rich-coaching.ts` | A missing or already archived tenant-scoped template is rejected; only the fenced state transition is audited. |
| `PATCH` | `/api/coaching/drills/[drillId]` | `setDrillTemplateFavourite` | `drill_template.favourite_changed` | `lib/rich-coaching.ts` | The expected-version fence, favourite-state update, version increment, and audit event commit atomically. |
| `PUT` | `/api/coaching/drills/[drillId]` | `updateDrillTemplate` | `drill_template.updated` | `lib/rich-coaching.ts` | A stale expected version commits neither the template update nor its audit event. |
| `POST` | `/api/coaching/drills/[drillId]/duplicate` | `duplicateDrillTemplate` | `drill_template.duplicated` | `lib/rich-coaching.ts` | The source remains unchanged; the new tenant-scoped template and its audit event commit together. |
| `POST` | `/api/coaching/drills/[drillId]/media` | `attachAccountMedia` | `media_attachment.created` | `lib/rich-coaching.ts` | The route first proves the tenant-owned drill; the ready tenant-owned asset, exact drill target, attachment metadata, and audit event then commit atomically. Attaching demo media does not rewrite existing practice-assignment snapshots. |
| `DELETE` | `/api/coaching/drills/[drillId]/media` | `withdrawAccountMediaAttachment` | `media_attachment.withdrawn` | `lib/rich-coaching.ts` | The route requires the active attachment to identify the exact tenant-owned drill before withdrawal. Missing, already withdrawn, cross-tenant, or wrong-drill identifiers commit neither state nor audit event. Existing assignment snapshots remain unchanged. |
| `POST` | `/api/coaching/roadmaps` | `createRoadmapTemplate` | `roadmap_template.created` | `lib/rich-coaching.ts` | The tenant-scoped roadmap-template insert and audit event commit in one D1 batch. |
| `DELETE` | `/api/coaching/roadmaps/[roadmapId]` | `archiveRoadmapTemplate` | `roadmap_template.archived` | `lib/rich-coaching.ts` | A missing or already archived tenant-scoped template is rejected; only the fenced state transition is audited. |
| `PATCH` | `/api/coaching/roadmaps/[roadmapId]` | `setRoadmapTemplateFavourite` | `roadmap_template.favourite_changed` | `lib/rich-coaching.ts` | The expected-version fence, favourite-state update, version increment, and audit event commit atomically. |
| `PUT` | `/api/coaching/roadmaps/[roadmapId]` | `updateRoadmapTemplate` | `roadmap_template.updated` | `lib/rich-coaching.ts` | A stale expected version commits neither the template update nor its audit event. |
| `POST` | `/api/coaching/roadmaps/[roadmapId]/duplicate` | `duplicateRoadmapTemplate` | `roadmap_template.duplicated` | `lib/rich-coaching.ts` | A stale source-template version commits neither the duplicate nor its audit event. |
| `POST` | `/api/consents` | `recordConsentTransition` | `consent.granted`<br>`consent.replaced`<br>`consent.withdrawn` | `lib/consent-repository.ts` | `[IDEMPOTENT NO-OP]` A same-payload, tenant-scoped key returns the immutable original transition and audit receipt. Changed payload returns conflict. The audit stores only the normalized input fingerprint and trusted request ID; it does not copy policy text, evidence references, the key, account identity, or golfer identifiers into metadata. Missing/invalid configuration blocks new grants but never blocks withdrawal of an existing current grant. |
| `POST` | `/api/data-export` | `createInstructorDataExport` | `data_export.generated`<br>`data_request.submitted` | `lib/data-export.ts` | `[IDEMPOTENT NO-OP]` An oversized workspace creates one open manual-fulfilment request; a retry returns that request without a duplicate submission event. |
| `POST` | `/api/data-requests` | `createAccountDataRequest` | `data_request.submitted`<br>`data_request.idempotency_alias` | `lib/repository.ts` | `[IDEMPOTENT NO-OP]` Every accepted key is tenant-scoped and bound to the normalized type/details payload: a same-payload replay returns its canonical request without another `data_request.submitted` event, while changed payload returns conflict. The hashed receipt is the audit-event ID (and new request ID); `request_id` remains the HTTP correlation ID. Distinct keys submitted while one deletion is open receive one minimized technical `data_request.idempotency_alias` receipt pointing to that canonical deletion; replaying an alias emits nothing new. |
| `PATCH` | `/api/operations/data-requests/[requestId]` | `transitionDataRequestOperatorStatus` | `data_request.operator_status_transitioned` | `lib/data-request-operations.ts` | `[IDEMPOTENT NO-OP]` A same-payload retry returns the immutable transition receipt without another event. The deterministic receipt ID is derived from the opaque request ID and idempotency key, while `request_id` remains the trusted HTTP correlation ID. Expected status plus `updatedAt` fence concurrent operators; a losing race or changed payload commits neither status nor audit. Only the non-attesting `submitted` -> `identity_verification_required` marker is available. Existing identity-verification-required, verified, in-progress, denied, cancelled, fulfilled, and failed states are read-only; the route cannot write verification evidence or advance processing. |
| `POST` | `/api/golfers` | `createGolferWorkspace` | `golfer_workspace.created` | `lib/repository.ts` | `[IDEMPOTENT NO-OP]` A replay of the same idempotency key returns the original workspace and audit receipt. |
| `POST` | `/api/golfers/staged` | `createStagedGolferWorkspace` | `golfer_workspace.staged` | `lib/repository.ts` | `[IDEMPOTENT NO-OP]` A replay of the same idempotency key returns the original staged workspace and audit receipt. |
| `DELETE` | `/api/golfers/[golferId]` | `archiveGolfer` | `golfer.archived` | `lib/golfer-lifecycle.ts` | — |
| `PUT` | `/api/golfers/[golferId]` | `updateGolferIdentity` | `golfer.identity_updated` | `lib/golfer-lifecycle.ts` | — |
| `POST` | `/api/golfers/[golferId]/complete` | `completeStagedGolferWorkspace` | `golfer_workspace.authoring_completed` | `lib/repository.ts` | `[IDEMPOTENT NO-OP]` A replay of the same idempotency key returns the original completion and audit receipt. |
| `POST` | `/api/media` | `storeMediaAsset` | `media.upload` | `lib/media.ts` | Private-object storage is followed by an atomic ready-state/audit commit. A storage or metadata-commit failure records `media.upload_failed`; metadata failure also attempts object compensation, and no successful-upload event is emitted. |
| `DELETE` | `/api/media/[mediaId]` | `deleteUnattachedMediaAsset` | `media.delete` | `lib/media.ts` | `[IDEMPOTENT NO-OP]` An already deleted tenant-owned asset emits no duplicate event. Attached profile, drill, roadmap, plan, phase, lesson, practice, evidence, review, milestone, launch-session, or launch-import media is rejected. The deleted-state/audit batch commits before best-effort private-object removal. |
| `POST` | `/api/packages` | `createCoachingPackage` | `coaching_package.created` | `lib/repository.ts` | `[IDEMPOTENT NO-OP]` A replay of the same idempotency key returns the original package and audit receipt. |
| `DELETE` | `/api/packages/[packageId]` | `archiveCoachingPackage` | `coaching_package.archived` | `lib/repository.ts` | — |
| `PUT` | `/api/packages/[packageId]` | `updateCoachingPackage` | `coaching_package.updated` | `lib/repository.ts` | — |
| `PUT` | `/api/plans/[planId]` | `editCorePlan` | `plan.core_edited` | `lib/plan-editor.ts` | — |
| `DELETE` | `/api/plans/[planId]/content` | `withdrawPlanContent` | `lesson.archive`<br>`practice.retire`<br>`evidence.withdraw`<br>`media_attachment.withdrawn` | `lib/plan-content.ts` | Withdrawing evidence also withdraws every active evidence-target media attachment and records one attachment audit per actual transition in the same revision-fenced batch. |
| `POST` | `/api/plans/[planId]/content` | `addCompletedLesson`<br>`addPracticeItem`<br>`addEvidenceItem`<br>`addPhaseReview` | `lesson.create`<br>`practice.create`<br>`practice.retire`<br>`evidence.create`<br>`phase_review.transition` | `lib/plan-content.ts` | `practice.retire` is conditional: a replacement emits exactly one event for each active practice item actually retired; an initial practice emits none. Stale, retried, or concurrent losing revisions commit neither content nor audit events. |
| `POST` | `/api/plans/[planId]/publish` | `publishPlanAndCreateShare` | `plan.publish_and_share` | `lib/plans.ts` | — |
| `POST` | `/api/plans/[planId]/coaching/launch/comparisons` | `createLaunchMonitorComparison` | `launch_monitor.comparison_created` | `lib/rich-coaching.ts` | The metric-pair snapshot, comparison group, plan revision fence/invalidation, and audit event commit in one D1 batch. |
| `POST` | `/api/plans/[planId]/coaching/launch/imports` | `stageLaunchMonitorImport` | `launch_monitor.import_staged` | `lib/rich-coaching.ts` | The bounded review rows, exact accepted rows/source row numbers, rejected-row reasons, mapping, confirmed units, optional ready source document, and failed/mapping-required/validated state persist as one resumable receipt. `[IDEMPOTENT NO-OP]` When supplied, the tenant-and-plan-scoped key hash returns the existing staged import on replay without a second audit event. Concurrent uniqueness races resolve to the existing import. |
| `POST` | `/api/plans/[planId]/coaching/launch/sessions` | `commitLaunchMonitorSession` | `launch_monitor.session_committed` | `lib/rich-coaching.ts` | For CSV imports, the server recomputes the fingerprint and derives every metric/shot from the persisted accepted-row snapshot and persisted source document; client-supplied metric rows cannot diverge from review. The session, normalized values, source-import transition, plan revision fence/invalidation, and audit event commit atomically. |
| `POST` | `/api/plans/[planId]/coaching/evidence` | `createEvidenceRecord` | `evidence.created`<br>`media_attachment.created` | `lib/rich-coaching.ts` | The typed evidence record, optional owned lesson/media references, plan revision fence/invalidation, and bounded audit metadata commit atomically. Media evidence requires a ready tenant-owned asset and atomically creates the active evidence-target attachment that governs golfer rendering and `/r/media` access; numeric evidence requires an exact metric name and unit. |
| `POST` | `/api/plans/[planId]/coaching/lessons` | `createLessonRecord` | `lesson.created` | `lib/rich-coaching.ts` | The lesson snapshot, plan revision fence/invalidation, and audit event commit atomically. |
| `PATCH` | `/api/plans/[planId]/coaching/lessons` | `transitionLessonLifecycle` | `lesson.revision_snapshotted`<br>`lesson.lifecycle_transitioned` | `lib/rich-coaching.ts` | Before any lifecycle or same-state correction, the full prior lesson narrative and bounded exact evidence/session IDs are stored immutably in the same revision-fenced batch. Invalid, oversized, cross-lesson, or stale selections commit neither lesson state, snapshot, nor audit event. |
| `POST` | `/api/plans/[planId]/coaching/media` | `attachPlanMedia` | `media_attachment.created` | `lib/rich-coaching.ts` | The exact-one-target attachment, plan revision fence/invalidation, and audit event commit atomically. |
| `DELETE` | `/api/plans/[planId]/coaching/media` | `withdrawPlanMediaAttachment` | `media_attachment.withdrawn` | `lib/rich-coaching.ts` | A missing, already withdrawn, cross-tenant, or stale-revision attachment commits no transition or audit event. |
| `POST` | `/api/plans/[planId]/coaching/milestones` | `createMilestone` | `milestone.created` | `lib/rich-coaching.ts` | The private milestone record, plan revision fence/invalidation, and audit event commit atomically; this is not a public or social share action. |
| `PATCH` | `/api/plans/[planId]/coaching/milestones` | `transitionMilestone` | `milestone.transitioned` | `lib/rich-coaching.ts` | Invalid, repeated, or stale milestone transitions commit neither state nor audit event. |
| `POST` | `/api/plans/[planId]/coaching/practice` | `assignDrillToPlan` | `practice.assigned` | `lib/rich-coaching.ts` | The immutable assignment snapshot, plan revision fence/invalidation, and audit event commit atomically; later drill-template edits cannot rewrite the assignment. |
| `PATCH` | `/api/plans/[planId]/coaching/practice` | `transitionPracticeAssignment`<br>`replacePracticeAssignment` | `practice.lifecycle_transitioned`<br>`practice.replaced` | `lib/rich-coaching.ts` | Invalid, repeated, or stale lifecycle transitions and assignment replacements commit neither state nor audit event; a successful replacement retains the prior snapshot and invalidates publication atomically. |
| `POST` | `/api/plans/[planId]/coaching/reviews` | `addPhaseReview` | `phase_review.transition` | `lib/plan-content.ts` | The explicitly selected tenant-and-plan-owned sources, coach conclusion/outcome, phase transition, plan revision fence/invalidation, and audit event commit in one D1 batch. Selected lessons carry a bounded immutable narrative/evidence/session snapshot and exact source plan revision, so later lesson corrections cannot rewrite what the review considered. A source-less, duplicate, oversized, foreign, unavailable, or stale review commits nothing. |
| `POST` | `/api/plans/[planId]/publish/replace-inaccessible` | `replaceInaccessiblePlanShare` | `share.replace_inaccessible_link` | `lib/plans.ts` | A stale revision, changed active-share ID, expired link, or concurrent losing replacement returns conflict and rolls back the complete D1 batch; it revokes no link or session, creates no replacement bearer, and emits no audit event. |
| `POST` | `/api/plans/[planId]/publish/reissue` | `reissuePublishedPlanShare` | `share.reissue_same_revision` | `lib/plans.ts` | Reissue requires current sharing consent, the exact already-published revision, exact last-share timestamp, and the exact latest revoked/expired history ID, status, and update timestamp. A live link, edit, revocation race, changed history head, or concurrent losing reissue rolls back every session/link change, insert, and audit event. |
| `PUT` | `/api/profile` | `saveProfile` | `profile.saved` | `lib/repository.ts` | — |
| `POST` | `/api/profile/media` | `attachAccountMedia` | `media_attachment.created` | `lib/rich-coaching.ts` | The tenant-owned ready asset, exact profile target, attachment, compatibility profile reference, and audit event commit atomically. |
| `DELETE` | `/api/profile/media` | `withdrawAccountMediaAttachment` | `media_attachment.withdrawn` | `lib/rich-coaching.ts` | A missing, already withdrawn, or cross-tenant profile attachment commits neither state nor audit event; compatibility profile references are cleared in the same batch. |
| `DELETE` | `/api/shares/[shareId]` | `revokeShareLink` | `share.revoke` | `lib/plans.ts` | `[IDEMPOTENT NO-OP]` An already inactive link changes no state and emits no duplicate revocation event. |
| `POST` | `/r/practice-check-in` | `recordPracticeCheckIn` | `practice.check_in_recorded` | `lib/rich-coaching.ts` | `[IDEMPOTENT NO-OP]` The key is scoped to the resolved account and live share session. A same-payload replay returns the immutable check-in without another row or event; changed payload returns conflict. Deterministic HMAC receipts bind the check-in and audit rows, while the raw key and golfer note are excluded from audit metadata. Session-context, publication, assignment, and current-consent fences are rechecked in the atomic write. |
| `POST` | `/r/response` | `recordGolferResponse` | `golfer.response_recorded` | `lib/plans.ts` | `[IDEMPOTENT NO-OP]` The required key is scoped to the resolved account and share session. A same-payload retry returns the immutable response without another response or audit row; changed payload returns conflict. Deterministic HMAC receipts bind the response and audit rows. The raw key is never persisted server-side or logged; a pending ambiguous attempt is retained only in per-tab browser session storage until a definitive result or tab closure. |
| `DELETE` | `/r/session` | `endShareSession` | `share.session_ended` | `lib/plans.ts` | `[TRANSPORT-ONLY]` When no live matching session exists, the route returns without a durable transition or audit event. It deliberately emits no `Set-Cookie`: a delayed close response cannot erase a newer session cookie, so an old revoked cookie may remain inert until the browser replaces or expires it. |
| `POST` | `/r/session` | `endShareSession`<br>`createShareSession` | `share.session_ended`<br>`share.session_created`<br>`share.access` | `lib/plans.ts` | The end event is conditional on replacing a live prior session; every successful new exchange emits the create and access events atomically. |
<!-- AUDIT_EVENT_MATRIX_END -->

## Boundary

The matrix covers exported `POST`, `PUT`, `PATCH`, and `DELETE` handlers under `app/**/route.ts`. Read-only handlers, scheduled recovery work, D1 leases, rate-limit counters, and provider receipt bookkeeping are outside the route/method inventory. Authorized data-request queue and single-request dry-run inventory reads are nevertheless recorded as `data_request.operator_queue_viewed` and `data_request.operator_detail_viewed`, with a trusted request ID and HMAC-derived operator reference but no email, free-text request detail, contact hash, object key, cursor, or excluded-row identifier. Queue metadata may include only bounded pagination facts and null-tenant/future-dated exclusion counts. That exclusion does not permit a durable business transition to hide as transport state: provider-confirmed Checkout expiry and completion-pending-sync are explicitly audited above without storing customer email, hosted URLs, provider session IDs, payloads, or payment details.

Practice replacement retirement events target the retired practice item and carry only a stable reason, a relationship label, and the opaque replacement practice-item ID. They share the replacement request ID and occurrence timestamp with the corresponding `practice.create` event. The audit insert, retirement update, replacement insert, revision fence, publication invalidation, and creation audit commit in one D1 batch, so a counted retirement cannot survive a rolled-back or stale replacement.

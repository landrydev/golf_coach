# Production findings and retest ledger

**Status:** Active release-control record under `AUTH-005`; last reconciled
2026-08-08
**Boundary:** The current recorded candidate is owner-only Sites version 9, bound
to immutable source commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d` and the
exact saved version and deployment below. Sites versions 8 and earlier are
superseded predecessor evidence and are not ordinary rollback targets after
consent-governed use. No private deployment is accepted public production.
**Related:** [Release evidence](RELEASE_EVIDENCE.md),
[Full-live completion audit](FULL_LIVE_COMPLETION_AUDIT.md),
[Security and privacy](SECURITY_PRIVACY.md), and
[owner release decisions](OWNER_RELEASE_DECISIONS_REQUIRED.md)

This ledger separates a confirmed defect or incident from a missing evidence item
or owner/external dependency. A severity on an evidence gap describes its release
effect; it does not assert that an undiscovered product defect exists. A document
edit does not close a hosted, manual, legal, real-user, or owner-acceptance item.

## Current exact-candidate evidence snapshot

| Field | Recorded Sites version 9 evidence |
|---|---|
| Source and runtime release ID | `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d` |
| Package | Local gzip SHA-256 `8b3d0b13f03f0b13cd10602d24af09bf17c34afdcb4cf73518b2b0d857d59e22`; 2,965,984 bytes; 61 tar entries. The exact archive verifier passed and found 10 migrations. Sites content `sha256:0b3986dc73b1d06539dc85900dfd959549d92bcceb812c231a418766d29411fb`; 49 files; 6,737,920 bytes. |
| Sites release | Saved version `appgprj_6a76957326fc819196ebf3a0c95f1ec3~appgver_58bb67e8e23c8191a584540a09e363c5`; deployment `appgdep_6a7768f92c588191934eda8abea6d6b4`; environment revision `11`; final status `succeeded`, provider `updated_at` `2026-08-08T17:36:53.329945+00:00` |
| Automated/local evidence | Production build, strict types, lint, and 229/229 tests passed; the exact release-integrity pass inspected 252 source text files with zero pattern findings and preserved Business Plan V1. The canonical Git-blob/SBOM `package-lock.json` SHA-256 remained `1b70e9ba538e5b990ef89578472d23887ed8a2cdff293a43615867fb2f43d69d`. Production dependency audit reported zero vulnerabilities. A fresh post-commit build's value-safe artifact verifier found 49 files, exactly two expected server-manifest credential copies, zero unexpected copies or paths, and no production prerender binding. A separate immutable-v9 clean install (501 packages) built and passed 229/229, but its `dist` did not byte-match the submitted archive after Windows CRLF checkout variance; deterministic byte identity remains open under `SUPPLY-EVID-001`. |
| Owner-private probes | Plaintext HTTP `/` redirected to HTTPS. Signed-out HTTPS requests to `/`, `/app`, `/api/health`, and `/api/operations/health` each returned the outer-policy `401` with `Cache-Control: no-store` and `Referrer-Policy: no-referrer`. These observations prove signed-out containment only; they do not exercise authenticated identity, application deep health, alerting, or a staffed observation window. |
| Configuration readiness | Consent-policy-registry and independent privacy-operator owner decisions/configuration remain absent. The corresponding controls fail closed, and deep readiness is therefore intentionally degraded rather than represented as ready. |
| Local browser evidence | Nine exact-commit synthetic Chrome captures cover landing, workspace, and golfer views at 320, 390, and 1440 CSS px. The initial 320 px golfer-header close-action defect was fixed and the complete nine-capture local retest passed without root/body horizontal overflow. The harness now exchanges the synthetic share capability through `/r/session` and uses the returned session cookie; it never stores the raw capability in a cookie. |
| Evidence still absent | No exact-v9 authenticated hosted browser journey, manual keyboard/screen-reader/accessibility review, supported-browser/device matrix, hosted scheduler/deep-health exercise, application rollback, D1/R2 restore, or measured RPO/RTO. Local synthetic captures do not supply those results. |

The automated, signed-out, and local synthetic-browser observations are exact-v9
evidence, but they do not
close any row whose closure requires authenticated, manual, hosted, external,
qualified-review, or owner-acceptance evidence.

## Classification and closure rules

| Class | Meaning |
|---|---|
| Critical | Current plausible unauthorized access, material data loss, unsafe billing, or similarly severe harm; do not expand affected operation while open |
| High | Release-blocking weakness or missing evidence for the affected public/real/paid operation |
| Medium | Material control limitation with containment; remediate or obtain explicit residual-risk acceptance before affected release |
| Low | Bounded issue with low expected harm; track to verified closure |
| Not applicable | Decision or dependency that needs resolution but is not itself a technical finding |

Allowed statuses are `OPEN`, `CONTAINED`, `REMEDIATED — RETEST PENDING`,
`CLOSED — RETEST PASSED`, and `ACCEPTED RESIDUAL RISK`. Only an exact-release
retest may produce `CLOSED — RETEST PASSED`. Residual-risk status requires Aaron's
dated decision, named owner, mitigation, review date, and stop/revisit trigger.

## Current ledger

| ID | Type / severity | Affected boundary and evidence | Current containment or remediation | Owner and status | Required closure and retest |
|---|---|---|---|---|---|
| `SEC-001` | Confirmed credential incident / **Critical** | A Sites project lookup returned a live SIWC bypass bearer credential in a private tool transcript for project `appgprj_6a76957326fc819196ebf3a0c95f1ec3`. The value is not reproduced or used. See the exact incident record in [release evidence](RELEASE_EVIDENCE.md#exact-private-sites-release). | Outer Sites access and application access remain owner-only. Prior values are treated as compromised. | Aaron authorizes the operation; release/security operators remain unassigned. **CONTAINED** | Record `OWNER-SEC-001`; rotate or revoke without recording values; prove the prior credential is denied and normal owner authentication still succeeds; sample logs for leakage. Do not expand access before this passes. |
| `SEC-003` | Confirmed build-credential exposure / **High** | The superseded version-7 build's generated prerender credential appeared in a private audit transcript. Its value is not reproduced or invoked in this evidence. | Version 9 was freshly built after exact source commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`. Its value-safe artifact verifier found exactly two expected server-manifest copies, zero unexpected copies or paths, and no production prerender binding; the exact submitted archive passed independently and the immutable candidate was deployed owner-only. Version 7 is superseded. | Release/security operators unassigned. **CLOSED — RETEST PASSED** | Closure for the build-generated prerender credential boundary was established on version 8 and passed current-candidate regression on exact private version 9. Preserve both retest rows below. Reopen on any unexpected credential copy/path, production prerender binding, reachable internal prerender route, archive/source mismatch, or reuse of a superseded artifact. This does not close `SEC-001`. |
| `SEC-002` | Design limitation / **Medium** | Exact Sites version 9 permits `'unsafe-inline'` in CSP `script-src` for framework bootstrap and denies inline script attributes with `script-src-attr 'none'`. Automated adversarial coverage passed, but the exact deployed CSP was not inspected in a supported browser. | Framework escaping, no raw HTML/SVG, input limits, URL allowlisting, no `unsafe-eval`, and `script-src-attr 'none'` reduce exposure. | Security owner unassigned. **OPEN** | Inspect the exact deployed CSP and adversarial renders in supported browsers; document whether vinext supports nonces/hashes; remove `'unsafe-inline'` when compatible or obtain time-bounded `OWNER-RISK-001` acceptance. |
| `AUTH-EVID-001` | Hosted evidence gap / **High before public access** | Exact-v9 SIWC sign-in, recovery, sign-out, stable identity, header-spoof denial, and cross-device behavior have not been demonstrated in the hosted configuration. Signed-out outer-policy `401` responses are not authenticated application evidence. | Owner-only outer policy and application HMAC allowlist fail closed. | Release/security operators unassigned. **OPEN** | After `SEC-001` is closed, complete an authenticated hosted matrix using authorized accounts, including forged/missing header denial and session termination; attach privacy-safe results to the exact release. |
| `RESP-001` | Confirmed narrow-viewport usability defect / **Medium** | During version-9 preparation, a local synthetic 320 CSS px golfer view showed the header close action compressed into an unusably narrow column. | The header now keeps tools non-shrinking, keeps the close label on one line, safely truncates the coach brand, and preserves the 320 px page width. A source regression plus fresh exact-commit 320/390/1440 local captures passed. | Quality/accessibility owner unassigned. **CLOSED — RETEST PASSED** (local synthetic scope only) | Closed only for the observed synthetic 320 px layout defect on commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`. Reopen on action clipping/wrapping, root overflow, or a corresponding hosted/manual failure. This does not close `A11Y-EVID-001`. |
| `HARNESS-001` | Confirmed test-evidence fidelity defect / **Medium** | The predecessor visual-review harness placed the raw synthetic share capability directly in the session-cookie slot instead of exercising the production capability-to-session exchange. This affected local evidence fidelity; no production path or customer data was involved. | The exact-v9 harness now POSTs the synthetic capability to `/r/session`, forwards only the returned scoped session cookie, and removes only its `Secure` transport attribute for loopback HTTP. A regression forbids a raw-capability cookie and the fresh nine-capture run passed. | Quality/security owner unassigned. **CLOSED — RETEST PASSED** (local synthetic scope only) | Closed for the local synthetic harness on commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`. It does not establish hosted golfer-session, browser-security, or manual evidence. Reopen if the harness bypasses `/r/session` or stores a capability as a cookie. |
| `A11Y-EVID-001` | Manual evidence gap / **High before public acceptance** | Exact-v9 local synthetic captures cover landing, workspace, and golfer views at 320, 390, and 1440 CSS px with no root/body horizontal overflow, and `RESP-001` is locally closed. Keyboard, focus, screen-reader, forced-colour, browser zoom, reduced-motion, long-content, supported-browser/device, and human review remain absent. | No claim of complete WCAG conformance is made; the local captures are explicitly synthetic and non-hosted. | Quality/accessibility owner unassigned. **OPEN** | Execute the approved manual matrix on the exact release, enter every defect here with severity, remediate release blockers, and retest with named tester, browser/assistive-technology versions, date, and artifacts. |
| `SUPPLY-EVID-001` | Deterministic-rebuild evidence gap / **Medium** | An isolated immutable-v9 export installed 501 packages and passed the full build/229-test suite, but its rebuilt `dist` did not byte-match the submitted archive. Windows CRLF checkout bytes changed migrations/metadata and content-hashed bundles. This is consistent with line-ending variance but does not prove line endings are the sole cause. | The submitted archive and runtime are immutably hashed; the original release working-tree build/archive comparison passed; behavior reproduces from a clean install. A repository LF rule was added after v9 and therefore is not current-runtime evidence. | Release/supply-chain owner unassigned. **OPEN** | On a later exact candidate containing the LF rule, perform an isolated clean checkout/install/build and exact archive comparison. Close only if byte identity passes or every remaining variance is explained, controlled, and explicitly dispositioned. |
| `OPS-EVID-001` | Recovery evidence gap / **High** | The exact-v9 local synthetic logical recovery exercise passed 10 migrations and 2 tenants (snapshot SHA-256 `34d14d9992bdae8b24d4504680f71ed00f5af2171152583fc40909ca89fd7a54`), but no controlled hosted D1/R2 restore or exact application rollback has been exercised. Provider durability is not treated as restore evidence. | Owner-only operation; recovery procedures exist; media upload is disabled. | Backup/recovery and release operators unassigned. **OPEN** | Complete [rollback and schema compatibility](ROLLBACK_SCHEMA_COMPATIBILITY.md), take a provider recovery point, exercise rollback separately from restore, measure RPO/RTO, verify tenant/capability/consent/audit integrity, and attach results. |
| `OPS-CRON-001` | Suspected hosted scheduler deployment gap / **High before paid operation** | The packaged v9 configuration declares `*/5 * * * *`, the Worker exports `scheduled()`, and local heartbeat tests pass. A predecessor-v8 provider-log query returned 24 events from `2026-08-08T16:37:26.325Z` through `2026-08-08T17:16:40.472Z`; all 24 were `fetch` and zero were `scheduled`, despite multiple expected five-minute boundaries. No exact-v9 hosted heartbeat or scheduled event is recorded. The provider log API's completeness, scheduled-event visibility, and deployed trigger metadata are undocumented/unavailable here, so this is a suspected gap, not a confirmed platform or application defect. | Checkout remains disabled and the site remains owner-only; no paid reconciliation depends on the scheduler in the current scope. | Operations/billing owners unassigned. **OPEN** | Establish whether Sites deploys the packaged cron and obtain provider-side trigger evidence. If unsupported, use an approved supported scheduler. On the resulting exact release, observe at least three expected intervals in authenticated operations health/provider evidence and exercise a privacy-safe failure alert before enabling paid operation. |
| `OPS-EVID-002` | Operational evidence gap / **High for paid operation; Medium while billing-disabled owner-only** | Exact-v9 authenticated deep health, alert delivery, cost monitoring, and staffed response have not been demonstrated. `OPS-CRON-001` separately records the suspected scheduler gap. Deep readiness is intentionally degraded because required consent and privacy-operator configuration is absent. | Checkout is disabled, consent and operator boundaries fail closed, and hosted scheduling is not relied on for current owner-only operation. | Operations/billing owners unassigned. **OPEN** | Resolve exact consent/operator owner decisions and configuration, then observe operational health in the exact environment; trigger privacy-safe test alerts; record receipt, escalation, response time, cost view, and corrective action. |
| `BILL-EVID-001` | External/owner dependency / **High for paid operation** | No approved offer, Product/Price, tax/refund/failure/cancel/pause policy, live credentials, publicly reachable signed webhook, or controlled real reconciliation/transaction evidence exists. | `BILLING_CHECKOUT_ENABLED=false`; production Stripe variables are absent from the recorded release. | Aaron plus billing operator, currently unassigned. **OPEN** | Record `OWNER-COMM-001` and `OWNER-PROD-001`; provision exact authorized configuration; pass Stripe test-mode failure/replay/order/recovery tests and one explicitly authorized controlled live exercise before enabling Checkout. |
| `PRIV-EVID-001` | Owner/external dependency / **High before real-user public operation** | Exact privacy notice, consent-policy registry entries, retention/deletion/backup-expiry schedule, instructor authority for golfer data, privacy-operator authority/configuration, contracting entity, contacts, and qualified Canadian review are absent. | Synthetic/owner-controlled use only; required consent and privacy-operator configuration fails closed; media is disabled; deletion is represented as a review request rather than completed deletion. | Aaron, privacy lead, and qualified reviewer; assignments unresolved. **OPEN** | Record `OWNER-OPS-001`, `OWNER-PRIV-001`, and `OWNER-MEDIA-001`; map versioned copy/policy to behavior and operator workflows; complete qualified review and deployed conformance check. |
| `VAL-EVID-001` | Real-world evidence gap / **High for full-live acceptance** | No authorized real instructor/golfer critical journey, unassisted activation result, support-burden record, or approved measurement protocol exists. | No demand, usability, price, retention, seasonality, or outcome claim is made from simulation. | Aaron and validation owner, currently unassigned. **OPEN** | Approve [the measurement packet](OWNER_MEASUREMENT_ACCEPTANCE_PACKET.md) and `OWNER-VALIDATION-001`; run the bounded adults-only protocol with approved consent/policy and record assistance/confounders. |
| `ACCEPT-001` | Owner decision / **Not applicable** | Aaron has not accepted an exact release, operating scope, configuration, policies, owners, or residual risks. | Deployment is described as an owner-only candidate, not an accepted public V1. | Aaron. **OPEN** | Complete the quality and risk packets, attach all exact-release evidence, then record `OWNER-ACCEPT-001` with date, scope, conditions, owners, and revisit triggers. |

## Retest log

Add one row per attempt; never replace a failed result with a later pass.

| Finding ID | Date/time and tester | Exact release / environment | Method and scope | Result | Evidence location | Follow-up |
|---|---|---|---|---|---|---|
| `SEC-003` | 2026-08-08; automated release workflow | Sites version 8; commit `cf117fef8ea42272d0b7e2358fe4197c024f86a7`; environment revision `10`; private deployment `appgdep_6a775b172534819196391fd626e95aa3` | Fresh post-commit build; value-safe artifact scan (49 files, two expected server-manifest copies, zero unexpected copies/paths, production prerender binding false); exact archive verification (gzip SHA-256 `99d615410c2e145e77938f0c5df4449ab8aeb4a544a5c5949fcdafa56a8e1378`, 10 migrations); private deployment; four signed-out containment probes | **PASS — CLOSED** for the build-generated prerender credential boundary. Signed-out probes to `/`, `/app`, `/api/health`, and `/api/operations/health` each returned `401`. | Exact version/archive/deployment identifiers in this ledger and [release evidence](RELEASE_EVIDENCE.md) | Keep version 7 superseded; reopen on a listed trigger. `SEC-001` remains independently contained/open pending `OWNER-SEC-001` rotation and proof. |
| `SEC-003` | 2026-08-08; automated release workflow | Sites version 9; commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`; environment revision `11`; private deployment `appgdep_6a7768f92c588191934eda8abea6d6b4` | Fresh exact-commit build; value-safe artifact scan (49 files, two expected server-manifest copies, zero unexpected copies/paths, production prerender binding false); exact archive verification (gzip SHA-256 `8b3d0b13f03f0b13cd10602d24af09bf17c34afdcb4cf73518b2b0d857d59e22`, 10 migrations); private deployment | **PASS — CLOSED REGRESSION** for the bounded build-generated prerender credential finding. | [Release evidence](RELEASE_EVIDENCE.md) and exact identifiers in this ledger | Keep the version-8 closure history and version 7 superseded. `SEC-001` remains independently contained/open. |
| `HARNESS-001` | 2026-08-08; automated/local synthetic-browser workflow | Commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`; loopback production-Worker harness | Source regression confirmed a POST to `/r/session`, use of its returned cookie, removal of only the `Secure` attribute for loopback HTTP, and absence of a raw-capability cookie pattern; a fresh nine-capture 320/390/1440 run completed | **PASS — CLOSED LOCALLY** for harness evidence fidelity; no hosted or customer path was tested. | [`ROADMAP-SITES-V9-2026-08-08-responsive-evidence.json`](release-evidence/ROADMAP-SITES-V9-2026-08-08-responsive-evidence.json) and automated release-document consistency test | Keep the regression. Do not use this result as hosted golfer-session or browser-security evidence. |
| `RESP-001` | 2026-08-08; local synthetic Chrome `151.0.7922.75` review | Commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`; loopback landing/workspace/golfer views | After the 320 px golfer close action was observed compressed, CSS preserved a non-shrinking header-tools region, one-line close action, and truncated brand; source regression plus nine fresh captures at 320/390/1440 recorded root/body width equal to viewport width | **PASS — CLOSED LOCALLY** for the observed narrow-layout defect. This was not a hosted, real-device, keyboard, screen-reader, or browser-zoom retest. | [`ROADMAP-SITES-V9-2026-08-08-responsive-evidence.json`](release-evidence/ROADMAP-SITES-V9-2026-08-08-responsive-evidence.json) and its nine digest-bound PNGs | Retain the manual accessibility and supported-browser/device matrix under `A11Y-EVID-001`. |
| `SUPPLY-EVID-001` | 2026-08-08; isolated exact-commit local workflow | Commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`; Windows, Node.js `v24.18.0`, npm `12.0.1` | Immutable export; `npm ci --no-audit` installed 501 packages; full verification passed 229/229; rebuilt `dist` compared with submitted v9 archive | **BEHAVIOR PASS / BYTE IDENTITY FAIL — OPEN.** CRLF checkout bytes corresponded to migration/metadata differences and different content-hashed bundles, but are not proven to be the only variance source. | [Exact v9 clean-install record](release-evidence/ROADMAP-SITES-V9-2026-08-08-clean-install.md) | Retest on a successor exact candidate containing the post-v9 LF rule; do not claim v9 deterministic byte reproducibility. |
| `OPS-CRON-001` | 2026-08-08; read-only provider-log observation | Predecessor Sites version 8 hosted window; version 9 carries the same packaged cron/Worker boundary but has no hosted scheduler observation | Provider query returned 24 events from `2026-08-08T16:37:26.325Z` through `2026-08-08T17:16:40.472Z`: 24 `fetch`, 0 `scheduled`. Local v9 package and tests confirm the cron declaration, `scheduled()` export, and heartbeat behavior only. | **INCONCLUSIVE — OPEN.** The absence is suspicious across multiple expected five-minute boundaries, but the returned set is not proven complete and the provider's scheduled-event visibility/deployed-trigger metadata are unavailable. | [Release evidence](RELEASE_EVIDENCE.md#predecessor-version-8-exact-record) and local scheduler/package evidence | Keep billing disabled. Establish provider cron support/metadata, then observe at least three exact-release intervals and a privacy-safe failure alert before paid operation. |

For closure, update the original row's status but preserve every retest row. Link the
exact command or manual protocol, immutable release identifier, environment,
expected and actual result, limitations, and reviewer. A release with any open
Critical finding, any un-dispositioned High finding applicable to its operating
scope, or an unapproved material residual risk is not accepted production evidence.

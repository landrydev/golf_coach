# Production findings and retest ledger

**Status:** Active release-control record under `AUTH-005`; last reconciled
2026-08-08
**Boundary:** The current recorded candidate is owner-only Sites version 11, bound
to immutable source commit `44670a64498779cf747914b4465380916a939301` and the
exact saved version and deployment below. Sites versions 9 and earlier are
superseded predecessor evidence. Version 10 is also superseded and is not an
ordinary rollback target because it would reintroduce script `'unsafe-inline'`.
No private deployment is accepted public production.
**Related:** [Release evidence](RELEASE_EVIDENCE.md),
[Full-live completion audit](FULL_LIVE_COMPLETION_AUDIT.md),
[Security and privacy](SECURITY_PRIVACY.md), and
[owner release decisions](OWNER_RELEASE_DECISIONS_REQUIRED.md)

This ledger separates a confirmed defect or incident from a missing evidence item
or owner/external dependency. A severity on an evidence gap describes its release
effect; it does not assert that an undiscovered product defect exists. A document
edit does not close a hosted, manual, legal, real-user, or owner-acceptance item.

## Current exact-candidate evidence snapshot

| Field | Recorded Sites version 11 evidence |
|---|---|
| Source and runtime release ID | `44670a64498779cf747914b4465380916a939301` |
| Package | Local gzip SHA-256 `d88be6513bc58afd057d4a3fb3a6d64b744f7a5c359731ec9fc693a788e1fa0e`; 2,966,073 bytes; 61 tar entries. The exact archive verifier passed and found ten migrations/49 files plus the exact scheduler declaration. Sites content `sha256:d717035871790252548e7fff4e1192e590b73b7cabfe3f4c011d65ffe4493daa`; 49 files; 6,737,920 bytes. |
| Sites release | Saved version `appgprj_6a76957326fc819196ebf3a0c95f1ec3~appgver_4c49cdec72bc8191aeece01f5689e51a`; deployment `appgdep_6a77b01714b881918245bb5248349e0d`; environment revision `13`; final status `succeeded`, provider `updated_at` `2026-08-08T22:40:07.742084+00:00` |
| Automated/local evidence | The main tree and two detached exact-commit clean worktrees passed build, strict types, lint, release-artifact integrity, and 237/237 tests. Each clean install added 501 locked packages with the same five blocked install scripts. The two 49-file builds had only the three strictly validated generated-value differences and zero normalized differences. Release integrity inspected 259 source/evidence text files with zero pattern findings, preserved Business Plan V1, and retained lockfile SHA-256 `1b70e9ba538e5b990ef89578472d23887ed8a2cdff293a43615867fb2f43d69d`; production audit reported zero vulnerabilities. The immutable-v9 byte-identity failure remains historical. |
| CSP nonce remediation | Exact-v11 automated production-bundle tests require one fresh 32-hex nonce per response, the matching nonce on every executable script, client CSP replacement, no script `'unsafe-inline'` or `'unsafe-eval'`, `script-src-attr 'none'`, and `no-store` on nonce-bearing public HTML. No supported signed-in hosted browser was available to inspect the deployed application response, so `SEC-002` remains retest pending. |
| Owner-private probes | From `2026-08-08T22:40:40.8612310Z` through `2026-08-08T22:40:42.0058063Z`, signed-out HTTPS requests to `/`, `/app`, `/api/health`, and `/api/operations/health` each returned the outer-policy `401` with `Cache-Control: no-store`, `Referrer-Policy: no-referrer`, and no redirect location. These observations prove signed-out containment only; they do not exercise authenticated identity, application deep health, CSP, alerting, or a staffed observation window. |
| Configuration readiness | Consent-policy-registry and independent privacy-operator owner decisions/configuration remain absent. The corresponding controls fail closed, and deep readiness is therefore intentionally degraded rather than represented as ready. |
| Local browser evidence | Nine version-9 synthetic Chrome captures cover landing, workspace, and golfer renderer/layout views at 320, 390, and 1440 CSS px. They remain a source-equivalent renderer/layout baseline only; v11 changes Worker/browser security behavior, so they are not exact-v11 CSP, header, hosted, or manual evidence. |
| Exact-v11 local recovery/capacity evidence | A detached worktree at exact deployed commit `44670a64498779cf747914b4465380916a939301` passed local synthetic recovery across ten migrations, 31/31 tables, two tenants, three private R2-compatible objects, three negative integrity scenarios, and child-process secret isolation. A bounded capacity run passed 54 requests at maximum concurrency four with zero failures. See the [exact local exercise record](release-evidence/ROADMAP-SITES-V11-2026-08-08-LOCAL-EXERCISES.md). |
| Evidence still absent | No exact-v11 authenticated hosted browser journey or CSP inspection, manual keyboard/screen-reader/accessibility review, supported-browser/device matrix, hosted scheduler/deep-health exercise, application rollback/forward-fix, provider-native D1/R2 restore, or measured RPO/RTO. Local synthetic exercises do not supply those results. |

The automated build/package and signed-out observations are exact-v11 evidence.
The local synthetic-browser observations are source-equivalent version-9 evidence,
limited to renderer/layout and not exact-v11 hosted/manual/security evidence. They do not
close any row whose closure requires authenticated, manual, hosted, external,
qualified-review, or owner-acceptance evidence.

## Classification and closure rules

| Class | Meaning |
|---|---|
| Critical | Current plausible unauthorized access, material data loss, unsafe billing, or similarly severe harm; do not expand affected operation while open |
| High | Release-blocking weakness or missing evidence for the affected public/real/paid operation |
| Medium | Material control limitation with containment; remediate or obtain explicit residual-risk acceptance before affected release |
| Low | Bounded issue with low expected harm; track to verified closure |
| Not applicable | Decision or dependency that needs resolution but is not itself a technical finding |

Allowed statuses are `OPEN`, `CONTAINED`, `REMEDIATED — RETEST PENDING`,
`CLOSED — RETEST PASSED`, and `ACCEPTED RESIDUAL RISK`. Only an exact-release
retest may produce `CLOSED — RETEST PASSED`. Residual-risk status requires Aaron's
dated decision, named owner, mitigation, review date, and stop/revisit trigger.

## Current ledger

| ID | Type / severity | Affected boundary and evidence | Current containment or remediation | Owner and status | Required closure and retest |
|---|---|---|---|---|---|
| `SEC-001` | Confirmed credential incident / **Critical** | A Sites project lookup returned a live SIWC bypass bearer credential in a private tool transcript for project `appgprj_6a76957326fc819196ebf3a0c95f1ec3`. The value is not reproduced or used. See the exact incident record in [release evidence](RELEASE_EVIDENCE.md#exact-private-sites-release). | Aaron authorized `OWNER-SEC-001` on 2026-08-08. One value-safe provider rotation ran from `2026-08-08T18:32:25.588Z` through `2026-08-08T18:32:31.831Z`; the connector contract immediately invalidates the prior token. The replacement was not displayed, persisted, or used. Pre/post access remained `custom`, revision 1, one owner, zero groups/external visitors; four post-rotation signed-out probes remained `401`/`no-store`/`no-referrer`. | Aaron authorized the operation; release/security operators remain unassigned. **REMEDIATED — RETEST PENDING** | The original value was not retained, so provider-contract invalidation is authoritative control-plane evidence rather than an empirical replay-denial probe. Complete a normal signed-in owner journey without a bypass header and obtain a meaningful privacy-safe hosted log/redaction sample. The empty post-operation log query is inconclusive. Do not expand access before this passes. |
| `SEC-003` | Confirmed build-credential exposure / **High** | The superseded version-7 build's generated prerender credential appeared in a private audit transcript. Its value is not reproduced or invoked in this evidence. | Exact version 11 was built twice from commit `44670a64498779cf747914b4465380916a939301`. Its value-safe artifact verifier found exactly two expected server-manifest copies, zero unexpected copies or paths, and no production prerender binding; the exact archive and provider package were recorded separately and the immutable candidate was deployed owner-only. Version 7 is superseded. | Release/security operators unassigned. **CLOSED — RETEST PASSED** | Closure was established on version 8 and passed regression on versions 9, 10, and 11. Preserve every dated retest row below. Reopen on any unexpected credential copy/path, production prerender binding, reachable internal prerender route, archive/source mismatch, or reuse of a superseded artifact. This is independent of `SEC-001`, which is remediated with retest pending. |
| `SEC-002` | Remediated design limitation / **Medium until hosted retest** | Exact Sites version 11 replaces script-element `'unsafe-inline'` with one fresh 32-hex response nonce in `script-src` and `script-src-elem`, propagates it to every vinext executable bootstrap script, retains `script-src-attr 'none'`, and has no `'unsafe-eval'`. The exact deployed application CSP was not inspected in a supported signed-in browser. | The trusted boundary replaces caller CSP/report-only headers; nonce-bearing public HTML is `no-store`; automated production-bundle tests require nonce shape, per-response uniqueness, script matching, and the absence of script `'unsafe-inline'`. Framework escaping, no raw HTML/SVG, input limits, and URL allowlisting remain in place. | Security owner unassigned. **REMEDIATED — RETEST PENDING** (hosted) | In a supported signed-in browser, inspect the exact-v11 response policy and executable scripts, exercise adversarial renders, and confirm normal navigation/hydration. Record only shape/matching results, never nonce values. Reopen immediately on a mismatch, script CSP violation, client-selected nonce, cached nonce-bearing HTML, or script `'unsafe-inline'`. |
| `AUTH-EVID-001` | Hosted evidence gap / **High before public access** | Exact-v11 SIWC sign-in, recovery, sign-out, stable identity, header-spoof denial, and cross-device behavior have not been demonstrated in the hosted configuration. Signed-out outer-policy `401` responses are not authenticated application evidence. | Owner-only outer policy and application HMAC allowlist fail closed. | Release/security operators unassigned. **OPEN** | After `SEC-001` is closed, complete an authenticated hosted matrix using authorized accounts, including forged/missing header denial and session termination; attach privacy-safe results to the exact release. |
| `RESP-001` | Confirmed narrow-viewport usability defect / **Medium** | During version-9 preparation, a local synthetic 320 CSS px golfer view showed the header close action compressed into an unusably narrow column. | The header now keeps tools non-shrinking, keeps the close label on one line, safely truncates the coach brand, and preserves the 320 px page width. A source regression plus fresh exact-commit 320/390/1440 local captures passed. | Quality/accessibility owner unassigned. **CLOSED — RETEST PASSED** (local synthetic scope only) | Closed only for the observed synthetic 320 px layout defect on commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`. Reopen on action clipping/wrapping, root overflow, or a corresponding hosted/manual failure. This does not close `A11Y-EVID-001`. |
| `HARNESS-001` | Confirmed test-evidence fidelity defect / **Medium** | The predecessor visual-review harness placed the raw synthetic share capability directly in the session-cookie slot instead of exercising the production capability-to-session exchange. This affected local evidence fidelity; no production path or customer data was involved. | The exact-v9 harness now POSTs the synthetic capability to `/r/session`, forwards only the returned scoped session cookie, and removes only its `Secure` transport attribute for loopback HTTP. A regression forbids a raw-capability cookie and the fresh nine-capture run passed. | Quality/security owner unassigned. **CLOSED — RETEST PASSED** (local synthetic scope only) | Closed for the local synthetic harness on commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`. It does not establish hosted golfer-session, browser-security, or manual evidence. Reopen if the harness bypasses `/r/session` or stores a capability as a cookie. |
| `A11Y-EVID-001` | Manual evidence gap / **High before public acceptance** | The exact-v9 local synthetic captures remain source-equivalent to v11 only for renderer/layout behavior and cover landing, workspace, and golfer views at 320, 390, and 1440 CSS px with no root/body horizontal overflow; `RESP-001` is locally closed. They are not relabelled exact-v11 CSP, header, hosted, or manual evidence. Keyboard, focus, screen-reader, forced-colour, browser zoom, reduced-motion, long-content, supported-browser/device, and human review remain absent. | No claim of complete WCAG conformance is made; the local captures are explicitly synthetic, renderer/layout-only, and non-hosted. | Quality/accessibility owner unassigned. **OPEN** | Execute the approved manual matrix on the exact release, enter every defect here with severity, remediate release blockers, and retest with named tester, browser/assistive-technology versions, date, and artifacts. |
| `SUPPLY-EVID-001` | Deterministic-rebuild evidence gap / **Medium** | The immutable-v9 rebuild failed byte identity after Windows CRLF checkout variance; that result remains historical and was not retroactively reproduced. Two distinct detached clean worktrees at exact version-11 commit `44670a64498779cf747914b4465380916a939301` each installed 501 locked packages, passed 237/237 tests, and produced identical 49-file inventories. The checked archive was then bound to saved version 11, its provider package, deployment, and environment revision 13. | A strict comparator allowed only one build UUID anchored in exactly three getter/ISR slots and exact single-property 64-hex prerender manifests whose pair matched within each build. Raw differences were confined to those three generated files and zero differences remained after allowlisted normalization. Five focused tests exercise the comparator's fail-open boundaries. | Release/supply-chain owner unassigned. **CLOSED — RETEST PASSED** (exact-v11 normalized-reproducibility scope) | Preserve the failed v9 and precursor-success retests below. This is normalized reproducibility, not byte-for-byte identity or retroactive v9 closure. Reopen on inventory drift, generated-value shape/location drift, any non-allowlisted raw difference, any normalized difference, or comparator-test regression. |
| `OPS-EVID-001` | Recovery evidence gap / **High** | Exact-v11 local synthetic logical recovery passed all 10 migrations, 31/31 application tables, 2 synthetic tenants, 3 private R2-compatible objects, three negative integrity scenarios, child-process secret isolation, and snapshot SHA-256 `34d14d9992bdae8b24d4504680f71ed00f5af2171152583fc40909ca89fd7a54`. No controlled hosted D1/R2 restore or exact application rollback/forward-fix has been exercised; provider durability is not treated as restore evidence. A v11-to-v10 rollback would also reintroduce script `'unsafe-inline'` and is not an ordinary class-`N` target. | Owner-only operation; exact-v11 local recovery procedures passed; media upload is disabled. | Backup/recovery and release operators unassigned. **OPEN** | Complete [rollback and schema compatibility](ROLLBACK_SCHEMA_COMPATIBILITY.md), take a provider recovery point, separately exercise an exact safe application rollback/forward-fix path, measure RPO/RTO, verify tenant/capability/consent/audit integrity, and attach results. |
| `OPS-CRON-001` | Suspected hosted scheduler deployment gap / **High before paid operation** | The exact-v11 archive verifier proves that packaged `server/wrangler.json` declares exactly `*/5 * * * *`; the Worker exports `scheduled()` and local heartbeat tests pass. Predecessor-v8/v9/v10 hosted observations supplied no scheduled event. The first post-deployment 30-minute aggregate at `2026-08-08T22:58:53.646Z` returned three `fetch`/`info`/`ok` and zero `scheduled` events. A newer 30-minute broad query and its `errors_only` companion around `2026-08-08T23:20:16.850Z` were empty. The later 60-minute query at approximately `2026-08-08T23:23:52Z` returned the same three `fetch`/`info`/`ok` events with HTTP `200`, `200`, and handled `403`, and `scheduled=0`; its `errors_only` result was only the handled `403`. No raw content was emitted or retained. Repeated samples strengthen suspicion, but remain inconclusive because provider-log completeness, scheduled-event visibility, and deployed trigger metadata are undocumented/unavailable. | Checkout remains disabled and the site remains owner-only; no paid reconciliation depends on the scheduler in the current scope. | Operations/billing owners unassigned. **OPEN** | Establish whether Sites deploys the packaged cron and obtain provider-side trigger evidence. If unsupported, use an approved supported scheduler. On the resulting exact release, observe at least three expected intervals in authenticated operations health/provider evidence and exercise a privacy-safe failure alert before enabling paid operation. |
| `OPS-EVID-002` | Operational evidence gap / **High for paid operation; Medium while billing-disabled owner-only** | Exact-v11 authenticated deep health, alert delivery, cost monitoring, and staffed response have not been demonstrated. `OPS-CRON-001` separately records the suspected scheduler gap. Deep readiness is intentionally degraded because required consent and privacy-operator configuration is absent. | Checkout is disabled, consent and operator boundaries fail closed, and hosted scheduling is not relied on for current owner-only operation. | Operations/billing owners unassigned. **OPEN** | Resolve exact consent/operator owner decisions and configuration, then observe operational health in the exact environment; trigger privacy-safe test alerts; record receipt, escalation, response time, cost view, and corrective action. |
| `BILL-EVID-001` | External/owner dependency / **High for paid operation** | No approved offer, Product/Price, tax/refund/failure/cancel/pause policy, live credentials, publicly reachable signed webhook, or controlled real reconciliation/transaction evidence exists. | `BILLING_CHECKOUT_ENABLED=false`; production Stripe variables are absent from the recorded release. | Aaron plus billing operator, currently unassigned. **OPEN** | Record `OWNER-COMM-001` and `OWNER-PROD-001`; provision exact authorized configuration; pass Stripe test-mode failure/replay/order/recovery tests and one explicitly authorized controlled live exercise before enabling Checkout. |
| `PRIV-EVID-001` | Owner/external dependency / **High before real-user public operation** | Exact privacy notice, consent-policy registry entries, retention/deletion/backup-expiry schedule, instructor authority for golfer data, privacy-operator authority/configuration, contracting entity, contacts, and qualified Canadian review are absent. | Synthetic/owner-controlled use only; required consent and privacy-operator configuration fails closed; media is disabled; deletion is represented as a review request rather than completed deletion. | Aaron, privacy lead, and qualified reviewer; assignments unresolved. **OPEN** | Record `OWNER-OPS-001`, `OWNER-PRIV-001`, and `OWNER-MEDIA-001`; map versioned copy/policy to behavior and operator workflows; complete qualified review and deployed conformance check. |
| `VAL-EVID-001` | Real-world evidence gap / **High for full-live acceptance** | No authorized real instructor/golfer critical journey, unassisted activation result, support-burden record, or approved measurement protocol exists. | No demand, usability, price, retention, seasonality, or outcome claim is made from simulation. | Aaron and validation owner, currently unassigned. **OPEN** | Approve [the measurement packet](OWNER_MEASUREMENT_ACCEPTANCE_PACKET.md) and `OWNER-VALIDATION-001`; run the bounded adults-only protocol with approved consent/policy and record assistance/confounders. |
| `ACCEPT-001` | Owner decision / **Not applicable** | Aaron has not accepted an exact release, operating scope, configuration, policies, owners, or residual risks. | Deployment is described as an owner-only candidate, not an accepted public V1. | Aaron. **OPEN** | Complete the quality and risk packets, attach all exact-release evidence, then record `OWNER-ACCEPT-001` with date, scope, conditions, owners, and revisit triggers. |

## Retest log

Add one row per attempt; never replace a failed result with a later pass.

| Finding ID | Date/time and tester | Exact release / environment | Method and scope | Result | Evidence location | Follow-up |
|---|---|---|---|---|---|---|
| `SEC-001` | `2026-08-08T18:32:25.588Z`–`2026-08-08T18:35:07.665Z`; automated `OWNER-SEC-001` workflow | Sites version 9; commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`; environment revision `11`; private deployment `appgdep_6a7768f92c588191934eda8abea6d6b4` | Recorded Aaron's authorization; value-safely compared pre/post access state; invoked one provider rotation whose contract immediately invalidates the prior token; discarded the undisplayed replacement; repeated four signed-out boundary probes; ran a 15-minute Worker-log query without emitting raw logs | **REMEDIATED — RETEST PENDING.** Rotation succeeded, owner-only access was unchanged, and all four signed-out probes returned `401`/`no-store`/`no-referrer`. The prior value was not retained or replayed; no signed-in owner browser was available; the log query returned zero events and is inconclusive for leakage/redaction. | [OWNER-SEC-001 rotation evidence](release-evidence/ROADMAP-SITES-V9-2026-08-08-sec001-rotation.md) | Complete a normal signed-in owner journey without a bypass header and a meaningful privacy-safe hosted log/redaction sample before closing or expanding access. Keep `AUTH-EVID-001` open for the broader hosted identity matrix. |
| `SEC-003` | 2026-08-08; automated release workflow | Sites version 8; commit `cf117fef8ea42272d0b7e2358fe4197c024f86a7`; environment revision `10`; private deployment `appgdep_6a775b172534819196391fd626e95aa3` | Fresh post-commit build; value-safe artifact scan (49 files, two expected server-manifest copies, zero unexpected copies/paths, production prerender binding false); exact archive verification (gzip SHA-256 `99d615410c2e145e77938f0c5df4449ab8aeb4a544a5c5949fcdafa56a8e1378`, 10 migrations); private deployment; four signed-out containment probes | **PASS — CLOSED** for the build-generated prerender credential boundary. Signed-out probes to `/`, `/app`, `/api/health`, and `/api/operations/health` each returned `401`. | Exact version/archive/deployment identifiers in this ledger and [release evidence](RELEASE_EVIDENCE.md) | Keep version 7 superseded; reopen on a listed trigger. `SEC-001` is tracked independently and is now remediated with retest pending. |
| `SEC-003` | 2026-08-08; automated release workflow | Sites version 9; commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`; environment revision `11`; private deployment `appgdep_6a7768f92c588191934eda8abea6d6b4` | Fresh exact-commit build; value-safe artifact scan (49 files, two expected server-manifest copies, zero unexpected copies/paths, production prerender binding false); exact archive verification (gzip SHA-256 `8b3d0b13f03f0b13cd10602d24af09bf17c34afdcb4cf73518b2b0d857d59e22`, 10 migrations); private deployment | **PASS — CLOSED REGRESSION** for the bounded build-generated prerender credential finding. | [Release evidence](RELEASE_EVIDENCE.md) and exact identifiers in this ledger | Keep the version-8 closure history and version 7 superseded. `SEC-001` is remediated with retest pending. |
| `HARNESS-001` | 2026-08-08; automated/local synthetic-browser workflow | Commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`; loopback production-Worker harness | Source regression confirmed a POST to `/r/session`, use of its returned cookie, removal of only the `Secure` attribute for loopback HTTP, and absence of a raw-capability cookie pattern; a fresh nine-capture 320/390/1440 run completed | **PASS — CLOSED LOCALLY** for harness evidence fidelity; no hosted or customer path was tested. | [`ROADMAP-SITES-V9-2026-08-08-responsive-evidence.json`](release-evidence/ROADMAP-SITES-V9-2026-08-08-responsive-evidence.json) and automated release-document consistency test | Keep the regression. Do not use this result as hosted golfer-session or browser-security evidence. |
| `RESP-001` | 2026-08-08; local synthetic Chrome `151.0.7922.75` review | Commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`; loopback landing/workspace/golfer views | After the 320 px golfer close action was observed compressed, CSS preserved a non-shrinking header-tools region, one-line close action, and truncated brand; source regression plus nine fresh captures at 320/390/1440 recorded root/body width equal to viewport width | **PASS — CLOSED LOCALLY** for the observed narrow-layout defect. This was not a hosted, real-device, keyboard, screen-reader, or browser-zoom retest. | [`ROADMAP-SITES-V9-2026-08-08-responsive-evidence.json`](release-evidence/ROADMAP-SITES-V9-2026-08-08-responsive-evidence.json) and its nine digest-bound PNGs | Retain the manual accessibility and supported-browser/device matrix under `A11Y-EVID-001`. |
| `SUPPLY-EVID-001` | 2026-08-08; isolated exact-commit local workflow | Commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`; Windows, Node.js `v24.18.0`, npm `12.0.1` | Immutable export; `npm ci --no-audit` installed 501 packages; full verification passed 229/229; rebuilt `dist` compared with submitted v9 archive | **BEHAVIOR PASS / BYTE IDENTITY FAIL — OPEN.** CRLF checkout bytes corresponded to migration/metadata differences and different content-hashed bundles, but are not proven to be the only variance source. | [Exact v9 clean-install record](release-evidence/ROADMAP-SITES-V9-2026-08-08-clean-install.md) | Retest on a successor exact candidate containing the post-v9 LF rule; do not claim v9 deterministic byte reproducibility. |
| `SUPPLY-EVID-001` | `2026-08-08T19:05:33.6134361Z`; automated isolated-worktree workflow | Undeployed successor source commit `66f5203a913f01c8da20555feebdbb99152c052c`; Windows, Node.js `v24.18.0`, npm `12.0.1` | Two distinct detached clean worktrees; each `npm ci --no-audit` installed 501 locked packages with five install scripts blocked; each `npm run verify` passed lint, typecheck, production build, artifact verification, and 234/234 tests with zero fail/skip/todo; strict comparison of the two isolated `dist` trees | **NORMALIZED REPRODUCIBILITY PASS — CLOSED PROSPECTIVELY.** Both inventories contained the same 49 files. Raw differences were limited to `server/index.js` and the two `vinext-server.json` manifests; the comparator validated the UUID in exactly three anchored getter/ISR slots and exact single-property 64-hex manifests with matching pairs per build. No values were recorded. Zero differences remained after allowlisted normalization. | [Successor normalized-reproducibility record](release-evidence/ROADMAP-SUPPLY-REPRO-2026-08-08.md) | Do not claim byte-for-byte identity, deployed-successor evidence, or retroactive reproduction of deployed v9 commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`. Preserve the five comparator boundary tests and reopen on a listed trigger. |
| `OPS-CRON-001` | 2026-08-08; read-only provider-log observation | Predecessor Sites version 8 hosted window; version 9 carries the same packaged cron/Worker boundary but has no hosted scheduler observation | Provider query returned 24 events from `2026-08-08T16:37:26.325Z` through `2026-08-08T17:16:40.472Z`: 24 `fetch`, 0 `scheduled`. Local v9 package and tests confirm the cron declaration, `scheduled()` export, and heartbeat behavior only. | **INCONCLUSIVE — OPEN.** The absence is suspicious across multiple expected five-minute boundaries, but the returned set is not proven complete and the provider's scheduled-event visibility/deployed-trigger metadata are unavailable. | [Release evidence](RELEASE_EVIDENCE.md#predecessor-version-8-exact-record) and local scheduler/package evidence | Keep billing disabled. Establish provider cron support/metadata, then observe at least three exact-release intervals and a privacy-safe failure alert before paid operation. |
| `SEC-003` | 2026-08-08; automated exact-candidate release workflow | Sites version 10; commit `ae35ef25ed46563f6b8f09f5c22dc12581eff8b1`; saved version `appgprj_6a76957326fc819196ebf3a0c95f1ec3~appgver_1007b9b4ea8c8191968d55991abf374c`; environment revision `12`; deployment `appgdep_6a779cabaec4819191b0cf1e815ce2e5` | Two exact-commit clean builds; value-safe artifact scan of 49 files with two expected server-manifest copies, zero unexpected copies/paths, and no production prerender binding; exact archive verification with gzip SHA-256 `5d67423e253009714bebe85bba118ded922c9f6b30b926f2af7bd0e3d05cd953`; provider package binding; private deployment and four signed-out containment probes | **PASS — CLOSED REGRESSION** for the bounded build-generated prerender credential finding. The probes each returned `401`/`no-store`/`no-referrer`; they are not authenticated security evidence. | [Exact version-10 evidence](release-evidence/ROADMAP-SITES-V10-2026-08-08.md) | Preserve the version-8 and version-9 closure history and keep version 7 superseded. `SEC-001` remains remediated with signed-in/log retest pending. |
| `SUPPLY-EVID-001` | 2026-08-08; automated exact-candidate isolated-worktree workflow | Sites version 10; commit `ae35ef25ed46563f6b8f09f5c22dc12581eff8b1`; Windows, Node.js `v24.18.0`, npm `12.0.1`; saved/deployed identifiers as above | Two distinct detached clean worktrees each installed 501 locked packages, reported the same five blocked install scripts, and passed 234/234 tests. Both `dist` inventories contained 49 files; strict comparison allowed differences only in three validated generated-value files and found zero normalized differences. The exact archive and provider package were separately bound to the successful revision-12 deployment. | **NORMALIZED REPRODUCIBILITY PASS — CLOSED FOR EXACT V10.** This is not byte-for-byte identity and does not rewrite the failed v9 result. | [Exact version-10 evidence](release-evidence/ROADMAP-SITES-V10-2026-08-08.md) | Preserve the v9 failure and precursor pass. Reopen on any comparator, inventory, generated-value-shape/location, archive-binding, or normalized-difference regression. |
| `OPS-CRON-001` | `2026-08-08T21:31:05.892Z`; value-safe provider-log observation | Sites version 10; commit `ae35ef25ed46563f6b8f09f5c22dc12581eff8b1`; deployment `appgdep_6a779cabaec4819191b0cf1e815ce2e5` | Queried the preceding 30-minute Worker-log window without emitting raw log content, 14m38s after final deployment began and after expected 21:20, 21:25, and 21:30 cron boundaries | **INCONCLUSIVE — OPEN.** The query returned zero events. Multiple expected boundaries strengthen suspicion, but the sample proves neither hosted scheduler invocation nor scheduler absence and does not establish log completeness, visibility, retention, redaction, error absence, or alerting. | [Exact version-10 evidence](release-evidence/ROADMAP-SITES-V10-2026-08-08.md#final-private-boundary-observations) | Keep billing disabled. Obtain provider trigger metadata or a supported scheduler, then observe at least three exact-release intervals through authenticated health/provider evidence and exercise a privacy-safe failure alert. |
| `SEC-002` | 2026-08-08; automated exact-candidate release workflow | Sites version 11; commit `44670a64498779cf747914b4465380916a939301`; environment revision `13`; deployment `appgdep_6a77b01714b881918245bb5248349e0d` | Two exact-commit clean builds and 237/237 tests; production-bundle adversarial checks required a fresh 32-hex CSP nonce per response, the matching nonce on every executable script, caller-CSP replacement, no script `'unsafe-inline'`/`'unsafe-eval'`, `script-src-attr 'none'`, and `no-store` on nonce-bearing public HTML; exact archive/provider binding and private deployment | **PASS LOCALLY / REMEDIATED — HOSTED RETEST PENDING.** The source and exact package no longer require script `'unsafe-inline'`. Signed-out outer-policy probes cannot reveal the application CSP, and no supported signed-in browser was mounted. | [Exact version-11 evidence](release-evidence/ROADMAP-SITES-V11-2026-08-08.md) | Inspect the exact live application policy/scripts and normal navigation in a supported signed-in browser without recording nonce values. Keep the finding retest pending until that passes. |
| `SEC-003` | 2026-08-08; automated exact-candidate release workflow | Sites version 11; commit `44670a64498779cf747914b4465380916a939301`; saved version `appgprj_6a76957326fc819196ebf3a0c95f1ec3~appgver_4c49cdec72bc8191aeece01f5689e51a`; environment revision `13`; deployment `appgdep_6a77b01714b881918245bb5248349e0d` | Two exact-commit clean builds; value-safe artifact scan of 49 files with two expected server-manifest copies, zero unexpected copies/paths, and no production prerender binding; exact archive verification with gzip SHA-256 `d88be6513bc58afd057d4a3fb3a6d64b744f7a5c359731ec9fc693a788e1fa0e`; provider package binding; private deployment and four signed-out containment probes | **PASS — CLOSED REGRESSION** for the bounded build-generated prerender credential finding. The probes each returned `401`/`no-store`/`no-referrer`; they are not authenticated security evidence. | [Exact version-11 evidence](release-evidence/ROADMAP-SITES-V11-2026-08-08.md) | Preserve the version-8 through version-10 closure history and keep version 7 superseded. `SEC-001` remains remediated with signed-in/log retest pending. |
| `SUPPLY-EVID-001` | 2026-08-08; automated exact-candidate isolated-worktree workflow | Sites version 11; commit `44670a64498779cf747914b4465380916a939301`; Windows, Node.js `v24.18.0`, npm `12.0.1`; saved/deployed identifiers as above | Two distinct detached clean worktrees each installed 501 locked packages, reported the same five blocked install scripts, and passed 237/237 tests. Both `dist` inventories contained 49 files; strict comparison allowed differences only in three validated generated-value files and found zero normalized differences. The exact archive and provider package were separately bound to the successful revision-13 deployment. | **NORMALIZED REPRODUCIBILITY PASS — CLOSED FOR EXACT V11.** This is not byte-for-byte identity and does not rewrite the failed v9 result. | [Exact version-11 evidence](release-evidence/ROADMAP-SITES-V11-2026-08-08.md) | Preserve the v9 failure and precursor/v10 passes. Reopen on any comparator, inventory, generated-value-shape/location, archive-binding, or normalized-difference regression. |
| `OPS-CRON-001` | `2026-08-08T22:58:53.646Z`; value-safe provider-log observation | Sites version 11; commit `44670a64498779cf747914b4465380916a939301`; deployment `appgdep_6a77b01714b881918245bb5248349e0d` | Verified the exact archive's scheduler declaration, then queried the preceding 30-minute Worker-log window without emitting raw events/messages, 19m23s after deployment success and after the 22:45, 22:50, and 22:55 expected boundaries. | **INCONCLUSIVE — OPEN.** Three events were `fetch`/`info`/`ok` and zero were observed as `scheduled`. An `errors_only` aggregate returned one `fetch`/`info`/`ok` event with zero error fields. Multiple expected boundaries strengthen suspicion, but the undocumented log surface proves neither hosted scheduler invocation nor absence, completeness, redaction, error absence, or alerting. | [Exact version-11 evidence](release-evidence/ROADMAP-SITES-V11-2026-08-08.md#final-private-boundary-observations) | Keep billing disabled. Obtain provider trigger metadata or a supported scheduler, then observe at least three exact-release intervals through authenticated health/provider evidence and exercise a privacy-safe failure alert. |
| `OPS-EVID-001` | 2026-08-08; exact-commit local synthetic recovery exercise | Sites version 11 runtime commit `44670a64498779cf747914b4465380916a939301`; disposable detached worktree; no hosted resource | Ran `npm run exercise:recovery:local` with lock-aligned dependencies; applied all ten migrations to isolated databases, populated and compared 31/31 tables for two tenants, restored three private R2-compatible objects, exercised three corruption/missing/checksum negatives, isolated secret-shaped parent variables, and removed synthetic state | **PASS LOCALLY / OPEN HOSTED.** Exact-v11 local logical recovery passed. The 103,656 ms local wall clock is not an RTO and snapshot age is not an RPO. | [Exact-v11 local exercise record](release-evidence/ROADMAP-SITES-V11-2026-08-08-LOCAL-EXERCISES.md) | Keep `OPS-EVID-001` open for provider-native backup/restore, application rollback/forward-fix, RPO/RTO, deletion recovery, alerting, and named-operator execution. |
| `OPS-CAP-001` | 2026-08-08; exact-commit bounded local synthetic capacity exercise | Sites version 11 runtime commit `44670a64498779cf747914b4465380916a939301`; disposable detached worktree; no hosted resource | Built the production Worker and ran 54 measured requests for two tenants at maximum concurrency four: four author/edit/publish/share flows, two exports, bounded reads, 44 `200`, ten `201`, zero failures; local p50 48.46 ms, p95 107.60 ms, max 107.83 ms | **PASS LOCALLY / NO CAPACITY CLAIM.** One disposable process does not establish a tenant limit, SLO/SLA, sustained load, soak, provider saturation, or hosted performance. | [Exact-v11 local exercise record](release-evidence/ROADMAP-SITES-V11-2026-08-08-LOCAL-EXERCISES.md) | Establish approved thresholds and run an authorized hosted capacity/reliability exercise before relying on performance for public or paid operation. |
| `OPS-CRON-001` | Approximately `2026-08-08T23:23:52Z`; repeated value-safe provider-log observation | Sites version 11; commit `44670a64498779cf747914b4465380916a939301`; deployment `appgdep_6a77b01714b881918245bb5248349e0d` | Queried a 60-minute aggregate and a newer 30-minute aggregate without emitting raw events/messages. The 60-minute result contained three `fetch`/`info`/`ok` events with HTTP `200`, `200`, and handled `403`; zero were observed as `scheduled`. `errors_only` returned the handled `403`; the newer 30-minute query was empty. | **INCONCLUSIVE — OPEN.** Repeated polling strengthens suspicion but cannot prove scheduler absence, log completeness/redaction, error absence, or alerting without scheduled-event visibility and deployed trigger metadata. | [Exact version-11 evidence](release-evidence/ROADMAP-SITES-V11-2026-08-08.md#final-private-boundary-observations) | Stop treating more of the same aggregate polling as closure evidence. Obtain provider trigger metadata or use a supported scheduler, then observe authenticated health across expected intervals and exercise alert delivery. |

For closure, update the original row's status but preserve every retest row. Link the
exact command or manual protocol, immutable release identifier, environment,
expected and actual result, limitations, and reviewer. A release with any open
Critical finding, any un-dispositioned High finding applicable to its operating
scope, or an unapproved material residual risk is not accepted production evidence.

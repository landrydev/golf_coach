# Production findings and retest ledger

**Status:** Active release-control record under `AUTH-005`; last reconciled
2026-08-09
**Boundary:** The current recorded candidate is owner-only Sites version 16,
bound to immutable source/runtime release commit
`91f37ebd542774779f6db7e000832c2f6714e528`, the exact package, the successful
private deployment, and environment revision `18` below. Sites version 15 is the
current failed `LOG-PRIV-001` provider-control evidence; version 14 is the first
failed retest, and version 13 and earlier remain predecessor evidence. Version 12
is not an ordinary rollback target because it lacks the
version-13 write-containment, session-
context recovery, strict receipt, share-lifecycle, profile-CAS, and close-abuse
controls. No private deployment is accepted public production.
Under `TECH-006`, Sites v16 is staging/evidence only and direct Cloudflare
Workers/D1/R2 is the un-deployed successor verification candidate. No direct-host
account, resource, configured or hosted OIDC provider, domain, deployment, or
acceptance evidence exists. The direct OIDC and revocable-session implementation
has local automated evidence only; see the
[direct successor pre-deployment record](release-evidence/ROADMAP-DIRECT-PREDEPLOY-2026-08-09.md).
**Related:** [Release evidence](RELEASE_EVIDENCE.md),
[Full-live completion audit](FULL_LIVE_COMPLETION_AUDIT.md),
[Security and privacy](SECURITY_PRIVACY.md), and
[owner release decisions](OWNER_RELEASE_DECISIONS_REQUIRED.md)

This ledger separates a confirmed defect or incident from a missing evidence item
or owner/external dependency. A severity on an evidence gap describes its release
effect; it does not assert that an undiscovered product defect exists. A document
edit does not close a hosted, manual, legal, real-user, or owner-acceptance item.

## Current exact-candidate evidence snapshot

| Field | Recorded Sites version 16 evidence |
|---|---|
| Source and runtime release ID | `91f37ebd542774779f6db7e000832c2f6714e528` |
| Package | Local gzip SHA-256 `9119a848bb8b4c7fff1d810280cf845ec44366449adac3176fd35d8c24438fe6` (3,052,294 bytes); 51 files; all 11 migrations through `0010_steep_hemingway`. Exact archive verification passed. Sites content is `sha256:752f05fd957f8f4b043b5955d9cdbdbf2176b0f1f3414827c9c3e8d0f44f6e2c` (7,290,880 bytes; 51 files). The packaged observability settings remain `observability.enabled=false`, `logs.enabled=false`, and `invocation_logs=false`. |
| Sites release and configuration | Saved version `appgprj_6a76957326fc819196ebf3a0c95f1ec3~appgver_0a4d7dc3d8108191aa4a1b3e14051a96`; deployment `appgdep_6a7826b2f4c481919cc85665dffa2391`; final status `succeeded` at `2026-08-09T07:05:36.176024Z`; environment revision `18`. `RELEASE_ID` is the exact commit above; `APPLICATION_WRITE_MODE=enabled`; `INSTRUCTOR_ACCESS_MODE=owner_private`; `BILLING_CHECKOUT_ENABLED=false`; and four existing secrets were retained. |
| Automated/local evidence | Two detached exact-commit clean worktrees passed 346/346 verification. Both builds contained 51 files; three allowlisted framework-generated raw differences and zero normalized differences were reported. Release integrity covered 304 files with zero secret findings, and the production audit reported zero vulnerabilities. The exact-v16 normalized supply-control scope is closed. |
| Exact-v16 hardening | Generic private/no-store HTML is limited to true top-level document `401`/`403`/`503`/`500` failures; API, RSC, route-handler, asset, and other programmatic clients keep JSON. CSP nonce, security-header, canonical-origin, access, and redirect ordering are preserved. Safe request references propagate only valid UUIDv4 values. Keyed-attempt v2 uses fixed 24-hour lifecycle, legacy-v1 retirement, a future-clock fence, exact-owner compare/remove, mount/submit expiry checks in all five keyed flows, and no automatic replay. |
| Readiness and scheduler | Deep readiness validates the exact migration-`0010` abuse-rate-limit table shape, primary key, required index, and both share-close scopes. Scheduler health samples at most 13 due accounts, reports a lower-bound backlog, remains degraded until drained, and fails closed on assessment error. These are exact local/source/package controls, not hosted scheduler or alert evidence. |
| Local recovery/capacity | The exact-v16 local recovery exercise restored 31/31 application tables and synthetic D1/R2 inventory, booted the exact built Worker, and authenticated profile, package, and workspace reads; operations health correctly reported the normalized interrupted scheduler as failed. This is local-only boot proof, not hosted restore, RPO, or RTO evidence. The bounded local capacity run completed 54 requests with zero failures and makes no hosted capacity claim. |
| Provider invocation-log finding | V15 remains the latest provider-log sample: despite all three packaged switches being off, the provider retained three `fetch` invocations and exposed nonempty network-IP/request-signature fields while cookie/SIWC identity fields were markers; collection/storage disposition remains unknown. No v16 provider-log query was run because the controls are unchanged and extra processing could not close the finding. `LOG-PRIV-001` remains High/open before any real-user or public operation. |
| Final-host suitability | Official Sites guidance prohibits enabling financial transactions, provides no data residency at launch, and warns that some background/hosting patterns are unsupported. `TECH-006` therefore supersedes Sites as the final paid live-V1 host while retaining v16 privately. Direct Cloudflare is selected only as the least-change candidate to verify. `HOST-SUIT-001` is contained, not closed. |
| CSP nonce remediation | Exact-v16 automated production-bundle tests retain one fresh 32-hex nonce per response, the matching nonce on every executable script, caller-CSP replacement, no script `'unsafe-inline'` or `'unsafe-eval'`, `script-src-attr 'none'`, and `no-store` on nonce-bearing public HTML. No supported signed-in hosted browser inspected the deployed application response, so `SEC-002` remains **REMEDIATED — HOSTED RETEST PENDING**. |
| Owner-private probes and credential scope | Fresh post-v16 signed-out HTTPS GETs to `/`, `/app`, `/r`, and `/api/health` each returned `401` with `no-store` and `no-referrer`; custom access remains one owner with no groups/external visitors. `OWNER-SEC-001` is historically complete, and no bypass operation or use occurred for v16. `SEC-001` remains **REMEDIATED — RETEST PENDING**. |
| Evidence still absent | No exact-v16 authenticated hosted browser/write journey, interruption/reload exercise, CSP inspection, manual accessibility review, hosted scheduler/recovery/alert evidence, rollback/forward-fix, provider-native D1/R2 restore, backup proof, measured RPO/RTO, or owner acceptance. `AUTH-EVID-001` remains open. |

The automated build/package and signed-out observations are exact-v16 evidence.
The local synthetic-browser observations are source-equivalent version-9 evidence,
limited to renderer/layout and not exact-v16 hosted/manual/security evidence. They do not
close any row whose closure requires authenticated, manual, hosted, external,
qualified-review, or owner-acceptance evidence.

The v16 package and supply controls passed, but `LOG-PRIV-001` is an open High
finding before any real-user or public operation. The current evidence does not prove public or paid launch,
owner acceptance, real-user safety, hosted signed-in behavior, manual accessibility,
provider scheduler/alert behavior, or recovery objectives.

## Classification and closure rules

| Class | Meaning |
|---|---|
| Critical | Current plausible unauthorized access, material data loss, unsafe billing, or similarly severe harm; do not expand affected operation while open |
| High | Release-blocking weakness or missing evidence for the affected public/real/paid operation |
| Medium | Material control limitation with containment; remediate or obtain explicit residual-risk acceptance before affected release |
| Low | Bounded issue with low expected harm; track to verified closure |
| Not applicable | Decision or dependency that needs resolution but is not itself a technical finding |

Allowed statuses are `OPEN`, `CONTAINED`, `REMEDIATED — RETEST PENDING`,
`CLOSED — RETEST PASSED`, and `ACCEPTED RESIDUAL RISK`. Only an exact-release
retest may produce `CLOSED — RETEST PASSED`. Residual-risk status requires Aaron's
dated decision, named owner, mitigation, review date, and stop/revisit trigger.

## Current ledger

| ID | Type / severity | Affected boundary and evidence | Current containment or remediation | Owner and status | Required closure and retest |
|---|---|---|---|---|---|
| `HOST-SUIT-001` | Confirmed final-host/provider constraint / **High before paid, real-user, or public operation** | Current official Sites guidance says not to use Sites to process card data or enable financial transactions, states that data residency is unavailable at launch, and warns that some background-service or hosting patterns are unsupported. Sites cron is also unproved, and `LOG-PRIV-001` remains High/open. These constraints contradict the paid live-V1 operating contract; they do not allege a customer incident. | `TECH-006` supersedes Sites as the final paid host, retains exact v16 owner-only with Checkout disabled and synthetic use only, and selects direct Cloudflare Workers/D1/R2 as the least-change verification candidate. No successor external resource has been created or changed. | Provider, release, privacy, and operations owners unassigned. **CONTAINED — SUCCESSOR RETEST PENDING** | On an authorized direct-Cloudflare account, deploy an exact private successor with approved public OIDC and provider/privacy baseline; prove invocation-log enforcement and retained-data disposition, cron/heartbeats/alerts, backup/restore/rollback, authenticated security/accessibility journeys, cost controls, and controlled billing. Obtain Aaron's exact-release acceptance before public/real/paid use. |
| `SEC-001` | Confirmed credential incident / **Critical** | A Sites project lookup returned a live SIWC bypass bearer credential in a private tool transcript for project `appgprj_6a76957326fc819196ebf3a0c95f1ec3`. The value is not reproduced or used. See the exact incident record in [release evidence](RELEASE_EVIDENCE.md#exact-private-sites-release). | Aaron authorized `OWNER-SEC-001` on 2026-08-08. One value-safe provider rotation ran from `2026-08-08T18:32:25.588Z` through `2026-08-08T18:32:31.831Z`; the connector contract immediately invalidates the prior token. The replacement was not displayed, persisted, or used. Pre/post access remained `custom`, revision 1, one owner, zero groups/external visitors; fresh v16 signed-out probes remained `401`/`no-store`/`no-referrer`. No bypass operation or use occurred for v16. | Aaron authorized the operation; release/security operators remain unassigned. **REMEDIATED — RETEST PENDING** | The original value was not retained, so provider-contract invalidation is authoritative control-plane evidence rather than an empirical replay-denial probe. Complete a normal signed-in owner journey without a bypass header after `LOG-PRIV-001` is resolved on a host that can produce a meaningful privacy-safe sample. Do not rotate again under the completed authorization, and do not expand access before both retests pass. |
| `LOG-PRIV-001` | Confirmed provider privacy/configuration limitation / **High before any real-user or public operation** | Value-safe provider samples after v14 and v15 deployment each retained three `fetch` invocation records despite exact packaged attempts to disable invocation logging. In v15, the query surface returned redaction markers for cookie and SIWC identity fields, but network-IP and request-signature fields were nonempty and not redaction markers; collection/storage disposition behind the markers is unknown. Connector/tool processing was transient; no raw field value was surfaced in the transcript or written to the repository. No customer-data incident is claimed. | Exact v16 retains the strongest available local settings: `observability.enabled=false`, `logs.enabled=false`, and `invocation_logs=false`. No v16 provider-log query was run because controls are unchanged and extra processing could not close the gap. The site remains owner-only, Checkout is disabled, all four signed-out routes return `401`/`no-store`/`no-referrer`, and no real users are authorized while this remains open. | Provider plus privacy/security operators unassigned. **OPEN — UNRESOLVED** | Obtain provider-side disablement, a complete retained-data disposition, and a value-safe sample proving invocation records are unavailable; or move to a supported host with enforceable privacy-safe logging and prove the same. A redaction-only proposal requires a complete field, collection, retention, access, deletion, and sample contract plus qualified privacy/security approval; redacting only the two observed field classes cannot close this finding. Deploy a new exact release and keep the site private with no real users until closure. |
| `SEC-003` | Confirmed build-credential exposure / **High** | The superseded version-7 build's generated prerender credential appeared in a private audit transcript. Its value is not reproduced or invoked in this evidence. | Exact version 16 was built twice from commit `91f37ebd542774779f6db7e000832c2f6714e528`; both clean verifications passed 346/346. The exact 51-file, 11-migration archive passed the release verifier and the immutable candidate was deployed owner-only. Version 7 is superseded. | Release/security operators unassigned. **CLOSED — RETEST PASSED** | Closure was established on version 8 and passed regression through version 16. Preserve every dated retest row below. Reopen on any unexpected credential copy/path, production prerender binding, reachable internal prerender route, archive/source mismatch, or reuse of a superseded artifact. This is independent of `SEC-001`, which is remediated with retest pending. |
| `SEC-002` | Remediated design limitation / **Medium until hosted retest** | Exact Sites version 16 retains version 11's per-response nonce CSP and limits generic private/no-store HTML failures to true top-level document navigation; API, RSC, route-handler, asset, and other programmatic clients keep JSON. The exact deployed application CSP was not inspected in a supported signed-in browser. | The trusted boundary preserves nonce/security/redirect ordering and replaces caller CSP/report-only headers; exact-v16 automated production-bundle tests require nonce shape, per-response uniqueness, script matching, and the absence of script `'unsafe-inline'`. | Security owner unassigned. **REMEDIATED — HOSTED RETEST PENDING** | In a supported signed-in browser, inspect the exact-v16 response policy and executable scripts, exercise document/API/RSC/asset failure boundaries and adversarial renders, and confirm normal navigation/hydration. Record only shape/matching results, never nonce values. |
| `RESP-IDEMP-001` | Confirmed response-durability defect / **Medium** | Version 11 could commit a golfer response and audit, lose the acknowledgement, then accept a retry as a second response/audit because `/r/response` had no operation-key contract or bounded client recovery. No real-user incident is claimed. | Version 12 requires a safe key; scopes HMAC receipts to account and resolved share session; atomically commits one response/audit; returns `201` first, `200` exact replay, and `409` changed input; covers same/mixed races; applies a 10-second timeout; preserves an ambiguous key per tab across reload; clears it after definitive outcome/tab close; uses fresh external-handoff keys; and never server-persists/logs the raw key. | Quality/security owner unassigned. **CLOSED — RETEST PASSED** (local automated scope) | Reopen on duplicate response/audit cardinality, cross-session replay, changed-input acceptance, raw-key persistence/logging, lost ambiguous key on reload, stale key after definitive result, or hosted/manual evidence contradicting the tested behavior. Hosted signed-in interruption/reload remains separate evidence. |
| `OPS-CONTAIN-001` | Confirmed operational containment limitation / **High during an integrity/security incident** | Version 12 lacked a single application-wide control that could stop conventional mutations, incidental-write `GET`/`HEAD` routes, and scheduled application work together. No production incident or unauthorized write is claimed. | Exact committed and privately deployed version 16 accepts only exact `APPLICATION_WRITE_MODE=enabled`; other values disable writes. The edge retains canonical/access ordering, returns generic private/no-store `503` HTML only for true top-level documents and JSON for programmatic clients, and keeps exact health/non-application reads outside the classifier. Scheduled work exits before D1/provider access. Two clean worktrees passed 346/346; revision `18` records exact `enabled`. | Operations/security owners unassigned. **REMEDIATED — RETEST PENDING** (exact source/package/deployed-configuration scope) | Exercise exact hosted `frozen`, missing/invalid, and restored `enabled` behavior with authenticated access; prove authentication ordering, generic/no-store response media behavior, zero scheduled D1/provider side effects while frozen, owner-only operations-health state, and one controlled post-enable write. |
| `CLIENT-RECOVERY-002` | Confirmed browser acknowledgement/recovery limitation / **Medium** | In version 12, browser mutations outside the specialized golfer-response flow did not share an end-to-end deadline or a uniform outcome-unknown contract. No duplicate-write or real-user incident is claimed. | Exact version 16 retains bounded full-response/no-replay behavior and adds UUIDv4-only request references. Keyed-attempt v2 records fixed 24-hour created/expires values, retires legacy v1, fences future clocks, uses exact-owner compare/remove, and rechecks expiry on mount and submit across all five keyed flows. Two clean exact-commit runs passed 346/346 and the exact package deployed privately. | Quality/security owners unassigned. **REMEDIATED — RETEST PENDING** (exact local/package/deployed-code scope) | Complete supported hosted interruption/reload review covering timeout/body/status failures, safe request-reference copy, v1/future/expired state, all five keyed flows, same-attempt retention, and no automatic replay. |
| `RESP-RECOVERY-002` | Confirmed cross-tab/session recovery defects / **High** | Version 12 stored ambiguous golfer attempts without an exact session context and could let a stale tab act through a replacement cookie; tokenless and failed-exchange cleanup could also disturb the prior session. No real-user incident is claimed. | Exact version 16 retains the non-reversible account/share/session context, stale-mismatch protection, atomic successful exchange, retryable preservation, and migration-`0010` close-abuse scopes. Keyed-attempt v2 lifecycle/ownership/expiry hardening applies to the response and close flows without automatic replay. Two exact clean runs passed 346/346 and the exact package deployed privately. | Quality/security owners unassigned. **REMEDIATED — RETEST PENDING** (exact local/package/deployed-code scope) | Complete the supported hosted two-tab/interruption/local-effect matrix, including v2 expiry/future-clock/legacy retirement and exact-owner removal. Do not close without authenticated hosted browser evidence on the exact release. |
| `AUTH-EVID-001` | Hosted evidence gap / **High before public access** | Exact-v16 SIWC sign-in, recovery, sign-out, stable identity, header-spoof denial, and cross-device behavior were not demonstrated in the hosted Sites configuration. For the direct successor, no provider tenant/client/callback or hosted sign-in exists. Signed-out outer-policy `401` responses and local synthetic OIDC tests are not authenticated hosted evidence. | Owner-only Sites policy and application HMAC allowlist fail closed. No bypass operation or use occurred during the v16 release. The direct successor now locally verifies authorization code, PKCE, state, nonce, issuer/audience/RS256 signature, one-time callback consumption, stable issuer-plus-subject mapping, no silent email merge, session rotation/revocation/capping, forged-header denial, and outage behavior; the complete suite passed 389/389. | Release/security operators unassigned. **OPEN** | On the selected supported host, configure an approved OIDC provider and complete discovery/JWKS/token/sign-in/linking/recovery/sign-out/revocation/spoof/cross-device/tenant tests with authorized accounts. Explicitly approve and test a legacy SIWC-to-OIDC identity-link ceremony if continuity is required; attach only privacy-safe results to the exact release. |
| `RESP-001` | Confirmed narrow-viewport usability defect / **Medium** | During version-9 preparation, a local synthetic 320 CSS px golfer view showed the header close action compressed into an unusably narrow column. | The header now keeps tools non-shrinking, keeps the close label on one line, safely truncates the coach brand, and preserves the 320 px page width. A source regression plus fresh exact-commit 320/390/1440 local captures passed. | Quality/accessibility owner unassigned. **CLOSED — RETEST PASSED** (local synthetic scope only) | Closed only for the observed synthetic 320 px layout defect on commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`. Reopen on action clipping/wrapping, root overflow, or a corresponding hosted/manual failure. This does not close `A11Y-EVID-001`. |
| `HARNESS-001` | Confirmed test-evidence fidelity defect / **Medium** | The predecessor visual-review harness placed the raw synthetic share capability directly in the session-cookie slot instead of exercising the production capability-to-session exchange. This affected local evidence fidelity; no production path or customer data was involved. | The exact-v9 harness now POSTs the synthetic capability to `/r/session`, forwards only the returned scoped session cookie, and removes only its `Secure` transport attribute for loopback HTTP. A regression forbids a raw-capability cookie and the fresh nine-capture run passed. | Quality/security owner unassigned. **CLOSED — RETEST PASSED** (local synthetic scope only) | Closed for the local synthetic harness on commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`. It does not establish hosted golfer-session, browser-security, or manual evidence. Reopen if the harness bypasses `/r/session` or stores a capability as a cookie. |
| `A11Y-EVID-001` | Manual evidence gap / **High before public acceptance** | The exact-v9 local synthetic captures remain historical renderer/layout evidence for unchanged UI/CSS only. They are not relabelled exact-v16 CSP, header, authentication, hosted, manual, or recovery-interaction evidence. Keyboard, focus, screen-reader, forced-colour, browser zoom, reduced-motion, long-content, supported-browser/device, and human review remain absent. | No claim of complete WCAG conformance is made; the local captures are explicitly synthetic, renderer/layout-only, and non-hosted. | Quality/accessibility owner unassigned. **OPEN** | Execute the approved manual matrix on the exact release, enter every defect here with severity, remediate release blockers, and retest with named tester, browser/assistive-technology versions, date, and artifacts. |
| `SUPPLY-EVID-001` | Deterministic-rebuild evidence gap / **Medium** | The immutable-v9 rebuild failed byte identity and remains historical. Two detached clean worktrees at exact-v16 commit `91f37ebd542774779f6db7e000832c2f6714e528` each passed 346/346; both had 51 files, three allowlisted raw differences, and zero normalized differences. The exact archive was bound to successful revision 18. | Strict comparator and artifact-integrity controls passed; archive SHA-256 `9119a848bb8b4c7fff1d810280cf845ec44366449adac3176fd35d8c24438fe6`; release integrity 304 files/zero findings; production audit zero vulnerabilities. | Release/supply-chain owner unassigned. **CLOSED — RETEST PASSED** (exact-v16 normalized-reproducibility/package scope) | Preserve the failed v9 and every predecessor retest below. This is not byte-for-byte identity or retroactive v9 closure. |
| `OPS-EVID-001` | Recovery evidence gap / **High** | Exact-v16 local recovery restored all 31/31 application tables and synthetic D1/R2 inventory, then booted the exact built Worker and authenticated profile, package, and workspace reads. Operations health correctly remained failed because the restored running scheduler was normalized to interrupted/failed. No hosted D1/R2 restore, provider backup proof, deletion recovery, rollback/forward-fix, or measured RPO/RTO has been exercised. | Owner-only operation; exact-v16 local restore-and-boot procedure passed; media upload is disabled. | Backup/recovery and release operators unassigned. **OPEN** | Complete [rollback and schema compatibility](ROLLBACK_SCHEMA_COMPATIBILITY.md), take a provider recovery point, exercise an exact safe rollback/forward-fix, measure RPO/RTO, verify integrity, and attach results. |
| `OPS-CRON-001` | Suspected hosted scheduler deployment gap / **High before paid operation** | The exact-v16 package declares `*/5 * * * *`. Deep readiness validates migration-`0010` abuse-rate-limit structure and scopes; scheduler health samples at most 13 due accounts, reports a lower-bound backlog, degrades until drained, and fails closed on assessment errors. Historical v8-v15 hosted observations supplied no conclusive scheduled event; no v16 provider-log query was run. No exact-v16 trigger metadata, authenticated heartbeat, frozen hosted exercise, or alert evidence exists. | Checkout remains disabled and the site remains owner-only. | Operations/billing owners unassigned. **OPEN** | Establish whether Sites deploys the cron or use a supported scheduler; after resolving `LOG-PRIV-001`, observe at least three intervals through authenticated health/privacy-safe evidence and exercise a failure alert. |
| `OPS-EVID-002` | Operational evidence gap / **High for paid operation; Medium while billing-disabled owner-only** | Exact-v16 authenticated hosted deep health, alert delivery, cost monitoring, and staffed response have not been demonstrated. Deep readiness intentionally remains degraded because required consent and privacy-operator configuration is absent. | Checkout is disabled; consent/operator boundaries fail closed; revision `18` records exact write mode `enabled`. Local deep-readiness and bounded scheduler-backlog tests do not establish hosted operation. | Operations/billing owners unassigned. **OPEN** | Resolve `LOG-PRIV-001` plus exact consent/operator decisions, then observe exact hosted health, alerts, costs, escalation, and corrective action. |
| `BILL-EVID-001` | External/owner dependency / **High for paid operation** | No approved offer, Product/Price, tax/refund/failure/cancel/pause policy, live credentials, publicly reachable signed webhook, or controlled real reconciliation/transaction evidence exists. | `BILLING_CHECKOUT_ENABLED=false`; production Stripe variables are absent from the recorded release. | Aaron plus billing operator, currently unassigned. **OPEN** | Record `OWNER-COMM-001` and `OWNER-PROD-001`; provision exact authorized configuration; pass Stripe test-mode failure/replay/order/recovery tests and one explicitly authorized controlled live exercise before enabling Checkout. |
| `PRIV-EVID-001` | Owner/external dependency / **High before real-user public operation** | Exact privacy notice, consent-policy registry entries, retention/deletion/backup-expiry schedule, instructor authority for golfer data, privacy-operator authority/configuration, contracting entity, contacts, and qualified Canadian review are absent. | Synthetic/owner-controlled use only; required consent and privacy-operator configuration fails closed; media is disabled; deletion is represented as a review request rather than completed deletion. | Aaron, privacy lead, and qualified reviewer; assignments unresolved. **OPEN** | Record `OWNER-OPS-001`, `OWNER-PRIV-001`, and `OWNER-MEDIA-001`; map versioned copy/policy to behavior and operator workflows; complete qualified review and deployed conformance check. |
| `VAL-EVID-001` | Real-world evidence gap / **High for full-live acceptance** | No authorized real instructor/golfer critical journey, unassisted activation result, support-burden record, or approved measurement protocol exists. | No demand, usability, price, retention, seasonality, or outcome claim is made from simulation. | Aaron and validation owner, currently unassigned. **OPEN** | Approve [the measurement packet](OWNER_MEASUREMENT_ACCEPTANCE_PACKET.md) and `OWNER-VALIDATION-001`; run the bounded adults-only protocol with approved consent/policy and record assistance/confounders. |
| `ACCEPT-001` | Owner decision / **Not applicable** | Aaron has not accepted an exact release, operating scope, configuration, policies, owners, or residual risks. | Deployment is described as an owner-only candidate, not an accepted public V1. | Aaron. **OPEN** | Complete the quality and risk packets, attach all exact-release evidence, then record `OWNER-ACCEPT-001` with date, scope, conditions, owners, and revisit triggers. |

## Retest log

Add one row per attempt; never replace a failed result with a later pass.

| Finding ID | Date/time and tester | Exact release / environment | Method and scope | Result | Evidence location | Follow-up |
|---|---|---|---|---|---|---|
| `SUPPLY-EVID-001` | 2026-08-09; automated exact-candidate isolated-worktree workflow | Sites version 16; commit `91f37ebd542774779f6db7e000832c2f6714e528`; environment revision `18`; deployment `appgdep_6a7826b2f4c481919cc85665dffa2391` | Two detached clean worktrees passed 346/346. Both builds contained 51 files; strict comparison found three allowlisted generated-value raw differences and zero normalized differences. Exact archive SHA-256 `9119a848bb8b4c7fff1d810280cf845ec44366449adac3176fd35d8c24438fe6` (3,052,294 bytes) was bound to the 51-file provider package and successful private deployment. Release integrity covered 304 files with zero findings; production audit found zero vulnerabilities. | **NORMALIZED REPRODUCIBILITY PASS — CLOSED FOR EXACT V16.** Not byte-for-byte identity. | Exact v16 commit/archive/version/deployment identifiers in this ledger | Preserve v15 and all predecessor rows. Reopen on any comparator, inventory, generated-value-shape/location, archive-binding, or normalized-difference regression. |
| `SEC-001` | 2026-08-09; value-safe exact-v16 private release workflow | Sites version 16; saved version `appgprj_6a76957326fc819196ebf3a0c95f1ec3~appgver_0a4d7dc3d8108191aa4a1b3e14051a96`; deployment above; revision `18` | No bypass operation or use occurred. Custom access remained one owner with no groups/external visitors. Signed-out `/`, `/app`, `/r`, and `/api/health` each returned `401`/`no-store`/`no-referrer`. | **REMEDIATED — RETEST PENDING.** Historical `OWNER-SEC-001` remains complete; normal signed-in owner evidence is absent. | Exact v16 configuration/deployment identifiers and historical rotation record | Do not rotate again under the completed authorization. Resolve `LOG-PRIV-001`, then complete the normal signed-in owner retest before expanding access. |
| `SEC-002`, `CLIENT-RECOVERY-002`, `RESP-RECOVERY-002` | 2026-08-09; automated exact-v16 workflow | Sites version 16; commit and deployment above | Tested top-level-document-only generic private/no-store HTML failures with JSON preserved for API/RSC/route-handler/assets/programmatic clients; nonce/security/redirect ordering; UUIDv4-only request references; and keyed-attempt v2 fixed lifecycle, v1 retirement, future-clock fence, exact-owner removal, mount/submit expiry across five flows, and no auto-replay. | **PASS LOCAL/PACKAGE/DEPLOYED CODE — HOSTED RETEST PENDING.** No signed-in hosted browser exercise occurred. | Exact-v16 automated suites and package/deployment binding | Complete the signed-in hosted CSP/failure-boundary and interruption/reload/multi-tab matrix before closure. |
| `OPS-EVID-001`, `OPS-CRON-001` | 2026-08-09; automated exact-v16 local workflow | Exact built version 16 Worker; local synthetic restored D1/R2 state only | Deep readiness validated exact migration-`0010` rate-limit shape/PK/index/scopes. Scheduler health sampled a maximum of 13 due accounts, reported lower-bound backlog, degraded until drained, and failed closed on assessment errors. Recovery restored 31/31 tables and D1/R2 inventory, booted the exact Worker, authenticated profile/package/workspace reads, and correctly reported normalized interrupted scheduler failure. | **PASS LOCALLY / OPEN HOSTED.** No hosted restore, scheduler, alert, RPO, or RTO claim. | Exact-v16 local readiness/scheduler/recovery evidence | Exercise provider-native restore, hosted scheduler/alerts, rollback/forward-fix, and measured objectives. |
| `OPS-CAP-001` | 2026-08-09; automated exact-v16 local workflow | Exact built version 16 Worker; disposable local synthetic state | Completed 54 requests with zero failures. | **PASS LOCALLY / NO HOSTED CAPACITY CLAIM.** | Exact-v16 local capacity evidence | Establish approved thresholds before any authorized hosted capacity exercise. |
| `LOG-PRIV-001` | 2026-08-09; v16 evidence decision | Sites version 16; unchanged packaged logging switches | No provider-log query was run because all three switches remained `false` and additional provider processing could not establish enforcement or retained-data disposition. | **NOT RETESTED — OPEN.** V15 remains the latest failed provider sample; no v16 pass is claimed. | V16 package configuration plus historical v15 failed retest below | Obtain provider enforcement/disposition or migrate, then perform a meaningful privacy-safe exact-release retest before real-user/public operation. |
| `LOG-PRIV-001` | `2026-08-09T05:32:32.228Z`–`2026-08-09T05:32:34.431Z`; value-safe exact-v15 provider retest | Sites version 15; commit `8a359398099ab9b970df1d28eb3473dcbcd6207f`; saved version `appgprj_6a76957326fc819196ebf3a0c95f1ec3~appgver_085298ff9b9c819193d48e0df7a71631`; deployment `appgdep_6a7810bf6fc08191b2cb9bfb081e58c2`; environment revision `17` | Verified the exact package configures `observability.enabled=false`, `logs.enabled=false`, and `invocation_logs=false`, then inspected only value-safe presence/redaction aggregates after deployment success. Three events shared one new script-version identity: two `fetch`/`ok`/`200` and one `fetch`/`ok`/`403`. The query surface returned redaction markers for cookie and SIWC identity fields; network-IP and request-signature fields were nonempty and not redaction markers. Collection/storage disposition behind the markers is unknown. Connector/tool processing was transient; no raw field value was surfaced in the transcript or written to the repository. | **FAIL — OPEN.** The stronger exact-v15 configuration was not enforced by the provider log surface. Current owner-only impact is bounded; no customer-data incident is claimed. | Exact v15 commit/package/deployment identifiers and this value-safe ledger record | Obtain provider-side disablement and retained-data disposition, or move to a supported host. Deploy a new exact release and pass a meaningful privacy-safe sample before any real-user/public operation. |
| `LOG-PRIV-001` | 2026-08-09; value-safe exact-v14 provider retest | Superseded Sites version 14; commit `5db791d0d4a7317a2913d2e65d855cb0bb31baec`; exact package set `invocation_logs=false` | Queried the post-deploy provider sample through transient connector/tool processing without surfacing raw header field values in the transcript or writing them to the repository. Three `fetch` events remained: two `200` and one handled `403`. | **FAIL — SUPERSEDED PREDECESSOR.** Configuring invocation logs off in the package did not prevent provider retention. | Exact v14 source/package and this value-safe ledger record | Preserve as the first failed retest. Version 15 is the stronger failed current retest; neither closes the finding. |
| `SUPPLY-EVID-001` | 2026-08-09; automated exact-candidate isolated-worktree workflow | Sites version 15; commit `8a359398099ab9b970df1d28eb3473dcbcd6207f`; environment revision `17` | Two detached exact-commit clean worktrees passed 333/333. Both builds contained 51 files; strict comparison found three expected generated-value raw differences and zero normalized differences. Exact archive verification passed for 51 files, all 11 migrations, and gzip SHA-256 `f987afcd00f9151e4c1a698fdf7aeb06fe8d275ec778494bb7dd38f535406a31`, then the package was bound to the successful private deployment. | **NORMALIZED REPRODUCIBILITY PASS — CLOSED FOR EXACT V15.** This is not byte-for-byte identity and does not rewrite the failed v9 result. | Exact v15 commit/archive/version/deployment identifiers in this ledger | Preserve all predecessor rows. Reopen on any comparator, inventory, generated-value-shape/location, archive-binding, or normalized-difference regression. |
| `SEC-001` | 2026-08-09; value-safe exact-v15 private release workflow | Sites version 15; commit `8a359398099ab9b970df1d28eb3473dcbcd6207f`; environment revision `17` | No SIWC bypass bearer was generated, rotated, read, displayed, persisted, or used. Signed-out HTTPS GETs to `/`, `/app`, `/r`, and `/api/health` each returned `401`/`no-store`/`no-referrer`; access remained owner-private. | **REMEDIATED — RETEST PENDING.** Historical `OWNER-SEC-001` rotation remains complete. The normal signed-in owner journey is still absent, and the provider-log sample failed privacy under `LOG-PRIV-001`. | Current exact identifiers and historical value-free rotation record | Do not rotate again under the completed authorization. Resolve `LOG-PRIV-001`, then complete the normal signed-in owner and privacy-safe hosted-log retest before expanding access. |
| `OPS-CONTAIN-001` | 2026-08-09; automated exact-candidate and private-deployment workflow | Sites version 13; commit `f3482845a42730e87f4ff1190550511f19ea6ad5`; environment revision `15`; owner-only; `APPLICATION_WRITE_MODE=enabled` | Two detached clean worktrees passed 332/332; the exact 51-file archive with all 11 migrations passed verification; edge/scheduled fail-closed controls are in the committed/deployed code; release/configuration records bind the exact enabled mode. | **PASS LOCAL/PACKAGE/DEPLOYED CONFIGURATION — RETEST PENDING.** Immutable source, package, deployment, and exact enabled configuration are proven. Hosted frozen/missing/invalid/restored-enabled behavior was not exercised behind authenticated access. | Exact version-13 release evidence and the application-write, scheduler, route-matrix, security, and critical-journey suites | Keep open until authenticated hosted freeze/invalid/re-enable testing proves generic/no-store response behavior, authentication ordering, zero scheduled D1/provider side effects, operations-health state, and one controlled post-enable write. |
| `CLIENT-RECOVERY-002` | 2026-08-09; automated exact-candidate and private-deployment workflow | Sites version 13; commit `f3482845a42730e87f4ff1190550511f19ea6ad5`; exact package; owner-only deployment | Two detached clean worktrees passed 332/332. Exact code/package coverage includes full-body deadlines and size bounds, strict receipts, no automatic replay, truthful unknown-outcome handling, and account/action/revision-scoped 24-hour/64-KiB/SHA-256 authoring drafts with explicit restore and CAS clear. | **PASS LOCAL/PACKAGE/DEPLOYED CODE — RETEST PENDING.** No supported signed-in hosted interruption, reload, storage-denial, or draft-recovery review was performed. | Exact version-13 release evidence and client mutation/keyed-attempt/authoring-draft recovery suites | Complete the exact hosted/manual recovery matrix before closure. |
| `RESP-RECOVERY-002` | 2026-08-09; automated exact-candidate and private-deployment workflow | Sites version 13; commit `f3482845a42730e87f4ff1190550511f19ea6ad5`; migration `0010_steep_hemingway`; owner-only deployment | Two clean exact-commit runs passed 332/332; exact source/package coverage includes session-context hashing/comparison, stale response/close rejection before mutation, cookie preservation, atomic successful replacement, retryable-exchange preservation, tokenless display-only behavior, definitive-success fallbacks, and independent `DELETE /r/session` network/session abuse scopes. | **PASS LOCAL/PACKAGE/DEPLOYED CODE — RETEST PENDING.** No supported signed-in hosted two-tab/interruption/browser-effects exercise was performed. | Exact version-13 release evidence, migration-upgrade tests, and client/critical-journey/security suites | Complete the hosted two-tab, interruption, cookie, cardinality, storage, and close-rate-limit matrix before closure. |
| `SEC-001` | 2026-08-09; value-safe exact-v13 private release workflow | Sites version 13; commit `f3482845a42730e87f4ff1190550511f19ea6ad5`; environment revision `15` | No SIWC bypass bearer was generated, read, displayed, persisted, or used. Signed-out HTTPS GETs to `/`, `/app`, `/r`, and `/api/health` each returned outer-policy `401`; access remained owner-private. | **REMEDIATED — RETEST PENDING.** The v13 release did not re-expose or use a bypass credential and retained signed-out containment, but it did not perform the required signed-in owner or meaningful hosted-log retest. | Exact version-13 release evidence and historical value-free rotation record | Keep `OWNER-SEC-001` historically complete and `SEC-001` open for the normal signed-in owner journey and privacy-safe hosted log/redaction sample. Do not rotate again from this authorization. |
| `SUPPLY-EVID-001` | 2026-08-09; automated exact-candidate isolated-worktree workflow | Sites version 13; commit `f3482845a42730e87f4ff1190550511f19ea6ad5`; environment revision `15` | Two detached clean worktrees each installed 501 locked packages with the same five blocked install scripts and passed 332/332. Both builds contained 51 files; strict comparison found three expected generated-value raw differences and zero normalized differences. Exact local archive verification passed for 51 files and 11 migrations and was bound to the saved/provider package and successful private deployment. | **NORMALIZED REPRODUCIBILITY PASS — CLOSED FOR EXACT V13.** This is not byte-for-byte identity and does not rewrite the failed v9 result. | [Exact v13 release evidence](release-evidence/ROADMAP-SITES-V13-2026-08-09.md) | Preserve all predecessor rows. Reopen on any comparator, inventory, generated-value-shape/location, archive-binding, or normalized-difference regression. |
| `OPS-CONTAIN-001` | 2026-08-09; automated local working-tree workflow | Uncommitted successor working tree; no release/version/deployment/archive assigned | Ran seven focused suites together with Node's type-transform test runner: exact mode parser/path classifier, scheduler/readiness no-work behavior, Worker route matrix and auth ordering, security validation, client recovery, and critical D1 journey. The combined run passed 59/59 with zero fail/skip/todo. | **PASS LOCALLY — RETEST PENDING.** Frozen/missing/malformed cases, incidental-write reads, all discovered instructor/API paths, generic responses, operations/public health separation, and zero scheduled D1/provider side effects passed locally. This is not exact-candidate or hosted evidence. | `tests/application-write-control.test.mjs`, `tests/scheduler-heartbeat.test.mjs`, `tests/security-route-matrix.test.mjs`, and current source | Commit an immutable candidate, run the full clean/reproducible release workflow, then exercise exact hosted freeze and controlled re-enable before closure. |
| `CLIENT-RECOVERY-002` | 2026-08-09; automated local working-tree workflow | Uncommitted successor working tree; no release/version/deployment/archive assigned | The same 59/59 focused run covered definitive/retryable responses, transport failure, pre-header and stalled-body timeout, 8 MiB overflow, bodyless responses, malformed/invalid successful JSON, definitive malformed/structured non-2xx JSON, copy variants, and the application client-call source inventory. | **PASS LOCALLY — RETEST PENDING.** The helper met the 10-second full-body/no-replay contract in local automated scope. No hosted interruption/reload or exact-build result is claimed. | `tests/client-mutation-recovery.test.mjs`, `tests/client-recovery.test.mjs`, and current client components | Repeat on the immutable clean candidate, then perform supported hosted/manual recovery checks before closure. |
| `RESP-RECOVERY-002` | 2026-08-09; automated local working-tree workflow | Uncommitted successor working tree; no release/version/deployment/archive assigned | The same 59/59 focused run covered context hashing/comparison, context-bound V3 recovery, contextless/malformed/storage-denied blocking, stale response/close `409`, response/audit cardinality, cookie preservation, invalid/throttled exchange preservation, successful replacement, tokenless display-only behavior, fragment handling, and definitive-success normal-link fallbacks. | **PASS LOCALLY — RETEST PENDING.** Unit/source/security checks and the critical D1 journey passed; no real browser multi-tab or hosted result is claimed. | `tests/client-recovery.test.mjs`, `tests/critical-journey.test.mjs`, `tests/security-validation.test.mjs`, and current share-session source | Repeat in the immutable clean candidate and complete the supported hosted two-tab/interruption/local-effect matrix before closure. |
| `SEC-001` | `2026-08-08T18:32:25.588Z`–`2026-08-08T18:35:07.665Z`; automated `OWNER-SEC-001` workflow | Sites version 9; commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`; environment revision `11`; private deployment `appgdep_6a7768f92c588191934eda8abea6d6b4` | Recorded Aaron's authorization; value-safely compared pre/post access state; invoked one provider rotation whose contract immediately invalidates the prior token; discarded the undisplayed replacement; repeated four signed-out boundary probes; ran a 15-minute Worker-log query without emitting raw logs | **REMEDIATED — RETEST PENDING.** Rotation succeeded, owner-only access was unchanged, and all four signed-out probes returned `401`/`no-store`/`no-referrer`. The prior value was not retained or replayed; no signed-in owner browser was available; the log query returned zero events and is inconclusive for leakage/redaction. | [OWNER-SEC-001 rotation evidence](release-evidence/ROADMAP-SITES-V9-2026-08-08-sec001-rotation.md) | Complete a normal signed-in owner journey without a bypass header and a meaningful privacy-safe hosted log/redaction sample before closing or expanding access. Keep `AUTH-EVID-001` open for the broader hosted identity matrix. |
| `SEC-003` | 2026-08-08; automated release workflow | Sites version 8; commit `cf117fef8ea42272d0b7e2358fe4197c024f86a7`; environment revision `10`; private deployment `appgdep_6a775b172534819196391fd626e95aa3` | Fresh post-commit build; value-safe artifact scan (49 files, two expected server-manifest copies, zero unexpected copies/paths, production prerender binding false); exact archive verification (gzip SHA-256 `99d615410c2e145e77938f0c5df4449ab8aeb4a544a5c5949fcdafa56a8e1378`, 10 migrations); private deployment; four signed-out containment probes | **PASS — CLOSED** for the build-generated prerender credential boundary. Signed-out probes to `/`, `/app`, `/api/health`, and `/api/operations/health` each returned `401`. | Exact version/archive/deployment identifiers in this ledger and [release evidence](RELEASE_EVIDENCE.md) | Keep version 7 superseded; reopen on a listed trigger. `SEC-001` is tracked independently and is now remediated with retest pending. |
| `SEC-003` | 2026-08-08; automated release workflow | Sites version 9; commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`; environment revision `11`; private deployment `appgdep_6a7768f92c588191934eda8abea6d6b4` | Fresh exact-commit build; value-safe artifact scan (49 files, two expected server-manifest copies, zero unexpected copies/paths, production prerender binding false); exact archive verification (gzip SHA-256 `8b3d0b13f03f0b13cd10602d24af09bf17c34afdcb4cf73518b2b0d857d59e22`, 10 migrations); private deployment | **PASS — CLOSED REGRESSION** for the bounded build-generated prerender credential finding. | [Release evidence](RELEASE_EVIDENCE.md) and exact identifiers in this ledger | Keep the version-8 closure history and version 7 superseded. `SEC-001` is remediated with retest pending. |
| `HARNESS-001` | 2026-08-08; automated/local synthetic-browser workflow | Commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`; loopback production-Worker harness | Source regression confirmed a POST to `/r/session`, use of its returned cookie, removal of only the `Secure` attribute for loopback HTTP, and absence of a raw-capability cookie pattern; a fresh nine-capture 320/390/1440 run completed | **PASS — CLOSED LOCALLY** for harness evidence fidelity; no hosted or customer path was tested. | [`ROADMAP-SITES-V9-2026-08-08-responsive-evidence.json`](release-evidence/ROADMAP-SITES-V9-2026-08-08-responsive-evidence.json) and automated release-document consistency test | Keep the regression. Do not use this result as hosted golfer-session or browser-security evidence. |
| `RESP-001` | 2026-08-08; local synthetic Chrome `151.0.7922.75` review | Commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`; loopback landing/workspace/golfer views | After the 320 px golfer close action was observed compressed, CSS preserved a non-shrinking header-tools region, one-line close action, and truncated brand; source regression plus nine fresh captures at 320/390/1440 recorded root/body width equal to viewport width | **PASS — CLOSED LOCALLY** for the observed narrow-layout defect. This was not a hosted, real-device, keyboard, screen-reader, or browser-zoom retest. | [`ROADMAP-SITES-V9-2026-08-08-responsive-evidence.json`](release-evidence/ROADMAP-SITES-V9-2026-08-08-responsive-evidence.json) and its nine digest-bound PNGs | Retain the manual accessibility and supported-browser/device matrix under `A11Y-EVID-001`. |
| `SUPPLY-EVID-001` | 2026-08-08; isolated exact-commit local workflow | Commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`; Windows, Node.js `v24.18.0`, npm `12.0.1` | Immutable export; `npm ci --no-audit` installed 501 packages; full verification passed 229/229; rebuilt `dist` compared with submitted v9 archive | **BEHAVIOR PASS / BYTE IDENTITY FAIL — OPEN.** CRLF checkout bytes corresponded to migration/metadata differences and different content-hashed bundles, but are not proven to be the only variance source. | [Exact v9 clean-install record](release-evidence/ROADMAP-SITES-V9-2026-08-08-clean-install.md) | Retest on a successor exact candidate containing the post-v9 LF rule; do not claim v9 deterministic byte reproducibility. |
| `SUPPLY-EVID-001` | `2026-08-08T19:05:33.6134361Z`; automated isolated-worktree workflow | Undeployed successor source commit `66f5203a913f01c8da20555feebdbb99152c052c`; Windows, Node.js `v24.18.0`, npm `12.0.1` | Two distinct detached clean worktrees; each `npm ci --no-audit` installed 501 locked packages with five install scripts blocked; each `npm run verify` passed lint, typecheck, production build, artifact verification, and 234/234 tests with zero fail/skip/todo; strict comparison of the two isolated `dist` trees | **NORMALIZED REPRODUCIBILITY PASS — CLOSED PROSPECTIVELY.** Both inventories contained the same 49 files. Raw differences were limited to `server/index.js` and the two `vinext-server.json` manifests; the comparator validated the UUID in exactly three anchored getter/ISR slots and exact single-property 64-hex manifests with matching pairs per build. No values were recorded. Zero differences remained after allowlisted normalization. | [Successor normalized-reproducibility record](release-evidence/ROADMAP-SUPPLY-REPRO-2026-08-08.md) | Do not claim byte-for-byte identity, deployed-successor evidence, or retroactive reproduction of deployed v9 commit `6b48fae48e8c9ddb87b1d7a8fd13a2ebe395ca0d`. Preserve the five comparator boundary tests and reopen on a listed trigger. |
| `OPS-CRON-001` | 2026-08-08; read-only provider-log observation | Predecessor Sites version 8 hosted window; version 9 carries the same packaged cron/Worker boundary but has no hosted scheduler observation | Provider query returned 24 events from `2026-08-08T16:37:26.325Z` through `2026-08-08T17:16:40.472Z`: 24 `fetch`, 0 `scheduled`. Local v9 package and tests confirm the cron declaration, `scheduled()` export, and heartbeat behavior only. | **INCONCLUSIVE — OPEN.** The absence is suspicious across multiple expected five-minute boundaries, but the returned set is not proven complete and the provider's scheduled-event visibility/deployed-trigger metadata are unavailable. | [Release evidence](RELEASE_EVIDENCE.md#predecessor-version-8-exact-record) and local scheduler/package evidence | Keep billing disabled. Establish provider cron support/metadata, then observe at least three exact-release intervals and a privacy-safe failure alert before paid operation. |
| `SEC-003` | 2026-08-08; automated exact-candidate release workflow | Sites version 10; commit `ae35ef25ed46563f6b8f09f5c22dc12581eff8b1`; saved version `appgprj_6a76957326fc819196ebf3a0c95f1ec3~appgver_1007b9b4ea8c8191968d55991abf374c`; environment revision `12`; deployment `appgdep_6a779cabaec4819191b0cf1e815ce2e5` | Two exact-commit clean builds; value-safe artifact scan of 49 files with two expected server-manifest copies, zero unexpected copies/paths, and no production prerender binding; exact archive verification with gzip SHA-256 `5d67423e253009714bebe85bba118ded922c9f6b30b926f2af7bd0e3d05cd953`; provider package binding; private deployment and four signed-out containment probes | **PASS — CLOSED REGRESSION** for the bounded build-generated prerender credential finding. The probes each returned `401`/`no-store`/`no-referrer`; they are not authenticated security evidence. | [Exact version-10 evidence](release-evidence/ROADMAP-SITES-V10-2026-08-08.md) | Preserve the version-8 and version-9 closure history and keep version 7 superseded. `SEC-001` remains remediated with signed-in/log retest pending. |
| `SUPPLY-EVID-001` | 2026-08-08; automated exact-candidate isolated-worktree workflow | Sites version 10; commit `ae35ef25ed46563f6b8f09f5c22dc12581eff8b1`; Windows, Node.js `v24.18.0`, npm `12.0.1`; saved/deployed identifiers as above | Two distinct detached clean worktrees each installed 501 locked packages, reported the same five blocked install scripts, and passed 234/234 tests. Both `dist` inventories contained 49 files; strict comparison allowed differences only in three validated generated-value files and found zero normalized differences. The exact archive and provider package were separately bound to the successful revision-12 deployment. | **NORMALIZED REPRODUCIBILITY PASS — CLOSED FOR EXACT V10.** This is not byte-for-byte identity and does not rewrite the failed v9 result. | [Exact version-10 evidence](release-evidence/ROADMAP-SITES-V10-2026-08-08.md) | Preserve the v9 failure and precursor pass. Reopen on any comparator, inventory, generated-value-shape/location, archive-binding, or normalized-difference regression. |
| `OPS-CRON-001` | `2026-08-08T21:31:05.892Z`; value-safe provider-log observation | Sites version 10; commit `ae35ef25ed46563f6b8f09f5c22dc12581eff8b1`; deployment `appgdep_6a779cabaec4819191b0cf1e815ce2e5` | Queried the preceding 30-minute Worker-log window without emitting raw log content, 14m38s after final deployment began and after expected 21:20, 21:25, and 21:30 cron boundaries | **INCONCLUSIVE — OPEN.** The query returned zero events. Multiple expected boundaries strengthen suspicion, but the sample proves neither hosted scheduler invocation nor scheduler absence and does not establish log completeness, visibility, retention, redaction, error absence, or alerting. | [Exact version-10 evidence](release-evidence/ROADMAP-SITES-V10-2026-08-08.md#final-private-boundary-observations) | Keep billing disabled. Obtain provider trigger metadata or a supported scheduler, then observe at least three exact-release intervals through authenticated health/provider evidence and exercise a privacy-safe failure alert. |
| `SEC-002` | 2026-08-08; automated exact-candidate release workflow | Sites version 11; commit `44670a64498779cf747914b4465380916a939301`; environment revision `13`; deployment `appgdep_6a77b01714b881918245bb5248349e0d` | Two exact-commit clean builds and 237/237 tests; production-bundle adversarial checks required a fresh 32-hex CSP nonce per response, the matching nonce on every executable script, caller-CSP replacement, no script `'unsafe-inline'`/`'unsafe-eval'`, `script-src-attr 'none'`, and `no-store` on nonce-bearing public HTML; exact archive/provider binding and private deployment | **PASS LOCALLY / REMEDIATED — HOSTED RETEST PENDING.** The source and exact package no longer require script `'unsafe-inline'`. Signed-out outer-policy probes cannot reveal the application CSP, and no supported signed-in browser was mounted. | [Exact version-11 evidence](release-evidence/ROADMAP-SITES-V11-2026-08-08.md) | Inspect the exact live application policy/scripts and normal navigation in a supported signed-in browser without recording nonce values. Keep the finding retest pending until that passes. |
| `SEC-003` | 2026-08-08; automated exact-candidate release workflow | Sites version 11; commit `44670a64498779cf747914b4465380916a939301`; saved version `appgprj_6a76957326fc819196ebf3a0c95f1ec3~appgver_4c49cdec72bc8191aeece01f5689e51a`; environment revision `13`; deployment `appgdep_6a77b01714b881918245bb5248349e0d` | Two exact-commit clean builds; value-safe artifact scan of 49 files with two expected server-manifest copies, zero unexpected copies/paths, and no production prerender binding; exact archive verification with gzip SHA-256 `d88be6513bc58afd057d4a3fb3a6d64b744f7a5c359731ec9fc693a788e1fa0e`; provider package binding; private deployment and four signed-out containment probes | **PASS — CLOSED REGRESSION** for the bounded build-generated prerender credential finding. The probes each returned `401`/`no-store`/`no-referrer`; they are not authenticated security evidence. | [Exact version-11 evidence](release-evidence/ROADMAP-SITES-V11-2026-08-08.md) | Preserve the version-8 through version-10 closure history and keep version 7 superseded. `SEC-001` remains remediated with signed-in/log retest pending. |
| `SUPPLY-EVID-001` | 2026-08-08; automated exact-candidate isolated-worktree workflow | Sites version 11; commit `44670a64498779cf747914b4465380916a939301`; Windows, Node.js `v24.18.0`, npm `12.0.1`; saved/deployed identifiers as above | Two distinct detached clean worktrees each installed 501 locked packages, reported the same five blocked install scripts, and passed 237/237 tests. Both `dist` inventories contained 49 files; strict comparison allowed differences only in three validated generated-value files and found zero normalized differences. The exact archive and provider package were separately bound to the successful revision-13 deployment. | **NORMALIZED REPRODUCIBILITY PASS — CLOSED FOR EXACT V11.** This is not byte-for-byte identity and does not rewrite the failed v9 result. | [Exact version-11 evidence](release-evidence/ROADMAP-SITES-V11-2026-08-08.md) | Preserve the v9 failure and precursor/v10 passes. Reopen on any comparator, inventory, generated-value-shape/location, archive-binding, or normalized-difference regression. |
| `OPS-CRON-001` | `2026-08-08T22:58:53.646Z`; value-safe provider-log observation | Sites version 11; commit `44670a64498779cf747914b4465380916a939301`; deployment `appgdep_6a77b01714b881918245bb5248349e0d` | Verified the exact archive's scheduler declaration, then queried the preceding 30-minute Worker-log window without emitting raw events/messages, 19m23s after deployment success and after the 22:45, 22:50, and 22:55 expected boundaries. | **INCONCLUSIVE — OPEN.** Three events were `fetch`/`info`/`ok` and zero were observed as `scheduled`. An `errors_only` aggregate returned one `fetch`/`info`/`ok` event with zero error fields. Multiple expected boundaries strengthen suspicion, but the undocumented log surface proves neither hosted scheduler invocation nor absence, completeness, redaction, error absence, or alerting. | [Exact version-11 evidence](release-evidence/ROADMAP-SITES-V11-2026-08-08.md#final-private-boundary-observations) | Keep billing disabled. Obtain provider trigger metadata or a supported scheduler, then observe at least three exact-release intervals through authenticated health/provider evidence and exercise a privacy-safe failure alert. |
| `OPS-EVID-001` | 2026-08-08; exact-commit local synthetic recovery exercise | Sites version 11 runtime commit `44670a64498779cf747914b4465380916a939301`; disposable detached worktree; no hosted resource | Ran `npm run exercise:recovery:local` with lock-aligned dependencies; applied all ten migrations to isolated databases, populated and compared 31/31 tables for two tenants, restored three private R2-compatible objects, exercised three corruption/missing/checksum negatives, isolated secret-shaped parent variables, and removed synthetic state | **PASS LOCALLY / OPEN HOSTED.** Exact-v11 local logical recovery passed. The 103,656 ms local wall clock is not an RTO and snapshot age is not an RPO. | [Exact-v11 local exercise record](release-evidence/ROADMAP-SITES-V11-2026-08-08-LOCAL-EXERCISES.md) | Keep `OPS-EVID-001` open for provider-native backup/restore, application rollback/forward-fix, RPO/RTO, deletion recovery, alerting, and named-operator execution. |
| `OPS-CAP-001` | 2026-08-08; exact-commit bounded local synthetic capacity exercise | Sites version 11 runtime commit `44670a64498779cf747914b4465380916a939301`; disposable detached worktree; no hosted resource | Built the production Worker and ran 54 measured requests for two tenants at maximum concurrency four: four author/edit/publish/share flows, two exports, bounded reads, 44 `200`, ten `201`, zero failures; local p50 48.46 ms, p95 107.60 ms, max 107.83 ms | **PASS LOCALLY / NO CAPACITY CLAIM.** One disposable process does not establish a tenant limit, SLO/SLA, sustained load, soak, provider saturation, or hosted performance. | [Exact-v11 local exercise record](release-evidence/ROADMAP-SITES-V11-2026-08-08-LOCAL-EXERCISES.md) | Establish approved thresholds and run an authorized hosted capacity/reliability exercise before relying on performance for public or paid operation. |
| `OPS-CRON-001` | Approximately `2026-08-08T23:23:52Z`; repeated value-safe provider-log observation | Sites version 11; commit `44670a64498779cf747914b4465380916a939301`; deployment `appgdep_6a77b01714b881918245bb5248349e0d` | Queried a 60-minute aggregate and a newer 30-minute aggregate without emitting raw events/messages. The 60-minute result contained three `fetch`/`info`/`ok` events with HTTP `200`, `200`, and handled `403`; zero were observed as `scheduled`. `errors_only` returned the handled `403`; the newer 30-minute query was empty. | **INCONCLUSIVE — OPEN.** Repeated polling strengthens suspicion but cannot prove scheduler absence, log completeness/redaction, error absence, or alerting without scheduled-event visibility and deployed trigger metadata. | [Exact version-11 evidence](release-evidence/ROADMAP-SITES-V11-2026-08-08.md#final-private-boundary-observations) | Stop treating more of the same aggregate polling as closure evidence. Obtain provider trigger metadata or use a supported scheduler, then observe authenticated health across expected intervals and exercise alert delivery. |
| `RESP-IDEMP-001` | 2026-08-09; automated exact-candidate release workflow | Sites version 12; commit `7b77e6507c1b1c1acb091ab046808cf8b5cc0a5c`; environment revision `14`; deployment `appgdep_6a77c5c85974819185ce1c8caf13007c` | Production-bundle and client tests covered missing/malformed keys, sequential replay, changed-payload conflict, same/mixed concurrent races, cross-session scope, response/audit cardinality, 10-second abort, per-tab ambiguous-outcome reload persistence/definitive cleanup, fresh external-handoff keys, and raw-key absence from persisted/logged evidence | **PASS — CLOSED LOCALLY.** First commit is `201`, identical replay `200`, changed input `409`, and one response/audit is committed per scoped operation key. | [Exact v12 release evidence](release-evidence/ROADMAP-SITES-V12-2026-08-09.md) and [critical journey](../tests/critical-journey.test.mjs), [client recovery](../tests/client-recovery.test.mjs), and [security validation](../tests/security-validation.test.mjs) | Preserve the regression and complete a supported signed-in hosted interruption/reload exercise before making hosted recovery claims. |
| `SUPPLY-EVID-001` | 2026-08-09; automated exact-candidate isolated-worktree workflow | Sites version 12; commit `7b77e6507c1b1c1acb091ab046808cf8b5cc0a5c`; revision `14`; deployment `appgdep_6a77c5c85974819185ce1c8caf13007c` | Two clean installs each added 501 packages with the same five blocked scripts and passed 242/242; both builds contained 49 files, three expected raw differences, and zero normalized differences; archive/provider binding passed | **NORMALIZED REPRODUCIBILITY PASS — CLOSED FOR EXACT V12.** Release integrity checked 261 files with zero findings; production audit found zero vulnerabilities. | [Exact v12 release evidence](release-evidence/ROADMAP-SITES-V12-2026-08-09.md) | Preserve v11's 237/237 and all earlier history unchanged. |
| `OPS-CRON-001` | `2026-08-09T00:13:27.8566565Z`; value-safe provider-log observation | Sites version 12; commit `7b77e6507c1b1c1acb091ab046808cf8b5cc0a5c`; deployment `appgdep_6a77c5c85974819185ce1c8caf13007c` | Queried broad and `errors_only` aggregates without emitting raw content | **INCONCLUSIVE — OPEN.** `errors_only` returned zero; broad returned six `fetch`/`info`/`ok` events—two `200` root, two `200` `/.rsc`, two handled `403` `/app.rsc`—and `scheduled=0`. Log completeness and scheduled visibility remain unknown. | [Exact v12 release evidence](release-evidence/ROADMAP-SITES-V12-2026-08-09.md) | Keep Checkout disabled; obtain trigger metadata or a supported scheduler plus authenticated health and alert evidence. |

For closure, update the original row's status but preserve every retest row. Link the
exact command or manual protocol, immutable release identifier, environment,
expected and actual result, limitations, and reviewer. A release with any open
Critical finding, any un-dispositioned High finding applicable to its operating
scope, or an unapproved material residual risk is not accepted production evidence.
